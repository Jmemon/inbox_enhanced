<!-- stamp: c0e22ae (feature/phase5-actions) | 2026-07-10 -->

# Reference Manifest

Master index of the `inbox_enhanced` reference corpus. Each reference doc is a
**dense navigational index** of one subsystem — file paths, key exports/types,
the services it talks to, the routes/tasks it owns, and the data flows across
process boundaries (API ↔ Postgres ↔ Redis ↔ Worker ↔ Google/Anthropic ↔
browser). They are maps, not tutorials.

## The matching contract

Agents (and humans) select docs by matching their task against the **Scope**
column — **not** by guessing from filenames. Bias toward over-inclusion: reading
an extra dense index is cheaper than missing a dependency. The `reference-lookup`
skill (`.claude/skills/reference-lookup/SKILL.md`) automates this routing.

- **File** — path to the reference doc, relative to `reference/`.
- **Stamp** — `<short-sha> (<branch>) | <YYYY-MM-DD>` the doc was last validated
  against code. Matches the top-of-file stamp inside each doc. A doc whose stamp
  lags the current `HEAD` by many commits should be verified against current code
  and re-stamped.
- **Scope** — one-line description of the subsystem the doc covers. This is the
  field you match a task against.

## Index

| File | Stamp | Scope |
|------|-------|-------|
| ACTIONS_INDEX.md | c0e22ae (feature/phase5-actions) | 2026-07-10 | Phase 5 actions (spec 006): tasks act on the inbox — deterministic per-task rules (`task_action_rules`, migration 0011, default-deny by absence, `trigger`∈{entity_entered_stage,thread_linked}/`action_type`∈{archive_thread,label_thread,draft_reply}/`mode`∈{propose,auto}) fire on APPLIED events or freshly-attached links and archive threads, apply Gmail labels, or draft (never send) a reply; the audit ledger (`task_actions`, exactly-one-of source_event_id/source_link_id CHECK, two partial-unique-index idempotency guards); the pure evaluator (`app/actions/rules.py`) + never-commits repo (`app/actions/repo.py`) + firing engine (`app/actions/engine.py`: fire_rules_for_event/fire_rules_for_link, commit-after-caller's-commit, `action_updated` SSE nudge, mode='auto'→Celery); the execution worker (`workers/action_tasks.py`: execute_action + the execute_action_inner seam the synchronous approve route shares); Gmail write-client additions (`gmail/client.py`: archive/label/draft/modify_thread_labels, scope-preflighted, zero send capability — tested invariant); rules CRUD + approve/reject/undo HTTP surface (`api/actions.py`); `has_write_scopes` auth state + `users.gmail_granted_scopes` (OAuth-callback-written); the typed `event\|action` `/api/reviews`/`/api/activity` union (`api/tasks.py`); client rules/action-review UI (`client/src/actions/`, `RulesSection`/`TaskActionsPanel`, HUD `ReviewTray`/`ActivityTicker` mixed-type rendering, the scope-migration banner). Cross-refs: TASKS_INDEX (task engine hooked into), WORKERS_INDEX (Celery/session conventions reused), INBOX_SYNC_INDEX (gmail_sync's fresh-link firing hook), CLIENT_INDEX (shared store/SSE conventions). |
| TASKS_INDEX.md | c0e22ae (feature/phase5-actions) | 2026-07-10 | Task engine (Phase 2A + 2B + 2C hardening + 3 aggregate feeds + 4 bucket unification + 4.5 jobs surface + **5 actions hooks**): EPS (Entity-Pipeline Schema) contract (`task_engine/schema.py`), append-only-event CRUD repo (`task_engine/repo.py`, sticky user-link rule, refold-from-events fold, **2B**: `recent_user_events`/`list_pending_events_for_thread`, **2C**: `delete_entity_if_orphaned` — reject's minted-entity cleanup, **3**: `list_pending_events_for_user`/`list_recent_events_for_user`/`get_entity_display_names` — cross-task, Task-joined, user-scoped, **4**: `list_active_buckets` — the task-backed bucket-list query), the pure 8-step extraction validator (`task_engine/transitions.py` — entity resolution via difflib not pg_trgm, correction fences, deferred-entity-mint `PendingCreate`, idempotency SELECT-first + partial-unique-index race backstop, **2B**: `pending_reason`/`proposed_entity` provenance, first-guard-wins), the one-LLM-call extraction orchestrator (`task_engine/engine.py`, **2B**: threads `recent_user_events` into the prompt), goal→draft cache (`task_engine/draft_cache.py`), criteria grammar shared with buckets (`task_engine/criteria.py`, **2B**: `append_example`/`cap_examples`/`EXAMPLE_CAP=30`/`_neutralize_tags` — spec §4.6 learning loop), the `triage_thread`/`extract_transition`(**2B**: float-confidence clamp)/`propose_task` LLM prompts, the decoupled `workers/task_engine_tasks.py` Celery module (process_task_updates/extract_for_thread/propose_task_draft/backfill_task — **4**: `backfill_task` is now kind-aware, branching tracker vs. bucket after a shared candidate-pool resolver; the new bucket branch, `_run_bucket_backfill`, triages against the full active bucket set and writes only `InboxThread.bucket_id`, no task_thread_links/task_events/extraction, with its own sync_lock-free optimistic write guard), the full `api/tasks.py` HTTP surface (goal→draft, tracker CRUD, board/events feed, attach/detach **2B**: `add_example` param + detach auto-rejects pending events, approve **2C**: re-dates `event.created_at` so the approval outranks a later refold/reject **2C**: cleans up an orphaned minted entity via `delete_entity_if_orphaned`/revert, manual state edit, entity merge; **2B**: uniform `HTTPException(422, str)` detail contract; **3**: `TaskSummary.stage_counts` histogram via `_stage_counts`, plus two new cross-task routes `GET /api/reviews`/`GET /api/activity`, both NOT under `/tasks*`; **4**: `POST /tasks` is kind-aware (`kind: 'tracker'|'bucket'`, 422 rules on `state_schema` presence), `GET /tasks?kind=` narrows tracker-only vs. bucket-only (defaults included), kind-conditional `criteria`/`is_default` list-item extras), `task_draft_ready`/`task_updated`/`task_backfill_progress` SSE events, `TASK_APPLY_CONFIDENCE`/`TASK_LINK_CONFIDENCE` gates, `tasks`/`task_thread_links`/`task_state_entities`/`task_events` Postgres tables (migration 0007; **2B** migration 0008 adds `task_events.pending_reason`/`proposed_entity`; **4** migration 0009 ID-preserving-copies every `buckets` row into `tasks(kind='bucket')`, retargets `inbox_threads.bucket_id`'s FK, drops `buckets` — the `Bucket` model is gone). **4**: `app/inbox/bucket_repo.py` survives as a task-backed shim (same 5 signatures); `api/buckets.py` is now a one-release CRUD-only shim ("Delete in Phase 5"); DELETED: `reclassify_user_inbox`/`_reclassify_all`/`draft_preview_bucket`/`preview_cache.py`/legacy `classify()`/`bucket_draft_preview` SSE event/`NewBucketModal.tsx`. Cross-ref (**3**): a second, hourly beat entry (`enqueue_tracker_owner_polls`) polls tracker owners with no open tab — full detail in WORKERS_INDEX/INBOX_SYNC_INDEX. **4.5 (jobs surface, spec 005)**: new `jobs` Postgres table (migration 0010; `Job` model — `kind` 'creation'\|'delete_retriage', `stage` machine per kind, `needs_user`, nested `payload` `{proposal:{name,description,state_schema,keyword_probes},positives,near_misses}`, `dismissed_at`-only dismissal — the string `'dismissed'` never occurs in `stage`) + `task_engine/jobs_repo.py` (never commits; `list_jobs` active-window = not dismissed AND (non-terminal OR terminal ≤7d)); new `api/jobs.py` (`POST /api/jobs`, `GET /api/jobs?active=`, `GET /api/jobs/{id}`, `POST /api/jobs/{id}/confirm` — 409 unless `draft_ready`+not-dismissed, shares `_create_task_from_fields` with `POST /api/tasks`, `POST /api/jobs/{id}/dismiss` — 204 idempotent); DELETED `POST/GET /tasks/draft*` + `task_engine/draft_cache.py` (drafts live in `jobs.payload`, never expire); `propose_task_draft(user_id, job_id, goal)` (renamed param, writes into the job row) and `backfill_task(..., job_id=None)` (per-batch job progress + terminal `stage`) both gain a top-level guarded-rollback + `_record_job_failure`-on-a-fresh-session failure path; **new** Celery task `retriage_deleted_bucket(user_id, job_id, deleted_task_id)` — bucket deletion re-triages orphaned threads (a no-fit/empty-set pick resolves to `NULL`, optimistic skip-if-moved write guard, no links/events/extraction) — the module docstring now documents TWO lock-free `InboxThread.bucket_id` writers (`_run_bucket_backfill`, `retriage_deleted_bucket`). New SSE event `job_updated {job_id}` (nudge); `task_draft_ready` retired; `task_backfill_progress` remains, consumed client-side only by `HudPage`'s tracker-card status (not `TaskDetail`). **5 (actions, spec 006)**: tracker tasks gain `task_action_rules`/`task_actions` (migration 0011) and hook points at every applied-event/fresh-link site (`process_task_updates`, `extract_for_thread`, `backfill_task`'s tracker branch, `approve_event`, `edit_entity_state` — structurally a no-op, `attach_thread`) plus `revert_event`/`detach_thread` auto-rejecting proposed actions; `merge_entity` and `refold_entity` fire nothing (structurally excluded); `/api/reviews`/`/api/activity` items gain a `"type": "event"|"action"` discriminator. Full detail (storage/evaluator/engine/worker/Gmail-writes/API/client) → `ACTIONS_INDEX.md`. |
| INBOX_SYNC_INDEX.md | c0e22ae (feature/phase5-actions) | 2026-07-10 | Three-way inbox sync: Gmail ↔ Postgres ↔ browser. Full sync = reconciling upsert (archives/un-archives, never wipes), partial sync mirrors Gmail archive/soft-delete/unread via widened `historyTypes` and self-heals `is_archived`/`is_deleted` from every live fetch (not just full sync), paginated `history.list` (`nextPageToken`, capped `MAX_HISTORY_PAGES`), extend sync, `gmail_last_history_id` cursor + `HistoryGoneError`, poll/full/extend Celery tasks classifying via `triage()` (Phase 2A — bucket pick + tracker relevance in one LLM call, dual-writing `task_thread_links`; sync/extend enqueue `process_task_updates` gated on content-touched/freshly-linked ids only — `(all_ids, content_ids)` split, flag-only touches publish but never extract — see TASKS_INDEX), `sync_lock`/`active_users` gates plus (**Phase 3**) a second, hourly `enqueue_tracker_owner_polls` beat entry that polls tracker owners with no open SSE tab — the 30s `active_users` path is no longer the only way a user gets polled — `last_sync:{uid}` freshness marker + `GET /api/sync/status`, `user:{uid}` pubsub→SSE push, `GET /api/search` (Postgres FTS/ILIKE), client `idLayer`/`displayLayer` LWW merge + auto-extend + archived-thread eviction + shared `useInboxSearch` (HudPage/InboxPage), shell-level `InboxProvider` (single `useInboxSse` subscription for the authed session). **Phase 4 (bucket unification)**: `reclassify_user_inbox` — the former 4th sync-adjacent Celery task, fired by bucket creation — is deleted along with its `_inline_reload`/`_reclassify_all` helpers; buckets are now `tasks(kind='bucket')`, and creating one enqueues the task engine's own `backfill_task` (kind='bucket' branch, own module, no sync_lock/Gmail touch) instead — full detail → TASKS_INDEX/WORKERS_INDEX. **Phase 4.5 (jobs surface)**: deleting a bucket-kind task with orphaned threads enqueues a SECOND lock-free `InboxThread.bucket_id` writer, `retriage_deleted_bucket` (own module, own optimistic write guard, no sync_lock/Gmail touch, no `last_sync` mark) — a THIRD `threads_updated`-publishing entrypoint outside the sync path proper, alongside poll/full/extend and a bucket's own creation-time backfill — full detail → TASKS_INDEX/WORKERS_INDEX. **Phase 5 (actions)**: `_write_task_links` now returns a `_LinkWriteResult(wrote, fresh)` (was a bare bool) — `fresh` is the subset of this round's link upserts where `newly_attached=True`; a new `_fire_link_rules` helper fires `thread_linked` action rules for each, called from `partial_sync_inbox`/`full_sync_inbox`/`extend_inbox_history` AFTER each function's own commit — full detail → ACTIONS_INDEX.md. |
| WORKERS_INDEX.md | c0e22ae (feature/phase5-actions) | 2026-07-10 | Celery worker + beat: celery_app factory (broker/backend=REDIS_URL, eager test mode), 30s beat tick → enqueue_polls fan-out, plus (**Phase 3**) a second, hourly beat tick → `enqueue_tracker_owner_polls` fan-out (DISTINCT active/non-deleted/schema-bearing tracker owners not already in `active_users`, `crc32(uid)%3600` countdown spread — covers owners with no open tab), tasks (poll_new_messages, full_sync_inbox_task, extend_inbox_history_task — **Phase 4**: down from 7 to 5, `draft_preview_bucket`/`reclassify_user_inbox` deleted), `last_sync:{uid}` freshness marker written on 5 sync-completion sites (read by `GET /api/sync/status`; was 6 pre-Phase-4), gmail_sync orchestration (reconciling full sync, widened-historyTypes partial sync) driven by `_triage_batch` (Phase 2A, replacing `_classify_batch` — bucket + tracker relevance in one call, dual-writes `task_thread_links`; `(all_ids, content_ids)` return split so extraction only sees content-touched threads, extend returns `new_link_ids`), the decoupled `workers/task_engine_tasks` Celery module fed by 5 enqueue-hook sites (incl. extend's freshly-linked enqueue; was 6 pre-Phase-4), sync_lock:{uid}, active_users zset gating, pubsub user:{uid} SSE fan-out, LLM via OpenRouter (LLM_CLASSIFY_MODEL/LLM_EXTRACT_MODEL/LLM_CONCURRENCY) with per-call llm_calls metrics persistence (app/llm/metrics.py). **Phase 4 (bucket unification)**: buckets are now `tasks(kind='bucket')`, no separate table/model; bucket creation (`POST /api/buckets`, a one-release shim, or `POST /api/tasks` with `kind='bucket'`) enqueues `workers/task_engine_tasks.py`'s `backfill_task` (its new kind='bucket' branch — pure reclassification, own optimistic write guard, no sync_lock) instead of the deleted `reclassify_user_inbox`; `draft_preview_bucket`/`preview_cache.py`/legacy `classify()` are deleted too, superseded by the task engine's own goal→draft flow (`propose_task_draft`, reusing `_read_candidates`/`_score_all` from this module via late import). Full task-engine detail → TASKS_INDEX.md. **Phase 4.5 (jobs surface)**: `propose_task_draft`'s `draft_id` param renamed `job_id` (writes into a `jobs` row instead of the deleted Redis `draft_cache`); `backfill_task` gains an optional `job_id` kwarg (per-batch job progress, terminal job `stage`); **new** task `retriage_deleted_bucket` (bucket-delete orphan re-triage — second lock-free `bucket_id` writer alongside `_run_bucket_backfill`); all three failure paths share a new `_record_job_failure` helper (fresh session) marking the job `stage='failed'`. `tests/conftest.py` now sets `CELERY_TASK_ALWAYS_EAGER` via `os.environ.setdefault` at its very first line (import-order freeze fix — celery_app reads it at first import, conftest always loads before test modules alphabetically). Full task-engine detail → TASKS_INDEX.md. **Phase 5 (actions)**: a new `workers/action_tasks.py` module (`execute_action` Celery task + the synchronous `execute_action_inner` seam the approve route shares); `task_engine_tasks.py`'s `process_task_updates`/`extract_for_thread`/`backfill_task` each gained a rule-firing hook after their own commit (batched `rules=` in the multi-event loop). Full detail → ACTIONS_INDEX.md. |
| CLIENT_INDEX.md | c0e22ae (feature/phase5-actions) | 2026-07-10 | React 19 + Vite browser SPA (client/src): react-router-dom routing (`AppShell` layout route wrapping `<Outlet>` in `state/InboxProvider.tsx` + (**2B**) `state/TasksProvider.tsx`, `/` HudPage, `/inbox` InboxPage, **2B**: `/tasks/:taskId` TaskDetail), App/AuthProvider, lib/api fetch wrappers (+ `searchInbox`, `getSyncStatus`, **2B**: full task-engine fetcher set + `throwWithDetail` 422-detail helper, **2C**: structured `ApiError` class with `.status`, **3**: `getReviews`/`getActivity` + `FeedItem` type + `TaskSummary.stage_counts`, **4**: dedicated bucket fetchers `createBucket`/`patchBucket`/`deleteBucket`/`postBucketDraftPreview`/`getBucketDraftPreview` all deleted — `getTasks({kind:'bucket'})`/`patchTask`/`deleteTask` take over, `createTask` gains `kind?`) + lib/sse EventSource singleton (**2B**: `task_draft_ready`/`task_updated`/`task_backfill_progress`; **4**: `bucket_draft_preview` member deleted), `InboxProvider`/`useInboxStore()` composing useBuckets/useInbox/useInboxSse **once** per authed session (shared store, no per-route remount-refetch; permanent `useInboxSse` subscription pins the SSE singleton; `useInbox`'s `autoExtendEnabled` route-gates auto-extend to `pathname==='/inbox'` so the session-wide instance can't fire at login/on the HUD; **4**: `useBuckets` is task-backed, its own `create` deleted), **2B**: `TasksProvider`/`useTasksStore()` — the analogous shared task store (task list + per-task detail cache + backfill progress), one SSE subscription refetching on a version-gap **OR** pending-count-gap (version alone is insufficient — several server mutations publish `task_updated` without bumping version), **2C**: 404 detection via `ApiError.status` (replacing a message-substring match), shared debounced (300ms) inbox search with monotonic race guard (`pages/search/`, **2B**: also powers `ThreadsPanel`'s search-to-attach), HudPage sync-freshness strip + store-derived bucket counts + (**2B**) task cards + new-task entry (**2C**: keyboard-accessible task cards + `agoLabel` NaN guard; **3**: per-card stage histogram + task-first layout — Tasks → `ReviewTray` → `ActivityTicker` → "Recently active" → Buckets), inbox list/pagination/reload, bucket filter/view modals (**4**: `NewBucketModal.tsx` deleted), **2B**: full task-engine UI (`pages/task/`: `NewTaskWizard`+`SchemaEditor` goal→draft→create flow — **4**: `NewTaskWizard` gains a `kind='tracker'|'bucket'` prop + `onCreated` callback and now also handles bucket creation, schema step skipped for buckets, `InboxPage` renders it in `NewBucketModal`'s place, **2C**: wizard surfaces draft-expiry and start failures inline, `TaskDetail` composition root with two-window pending-review events fetch, `PipelineBoard`, `EntityDrawer`, `ReviewFeed`, `ThreadsPanel`, **3**: `pendingReasonCopy` extracted to shared `pendingReasons.ts`), **3**: new HUD-level `ReviewTray`/`ActivityTicker` (self-owned data, `/api/reviews`/`/api/activity`, refetch on summed pending/version signals from `TasksProvider`); the Browser↔API fetch+SSE realtime layer. **4**: `InboxPage`'s `createWithWatchdog` (60s+150s reclassify watchdog) deleted — bucket backfill's `threads_updated` publish is deterministic. **4.5 (jobs surface, spec 005)**: new `state/JobsProvider.tsx` (`JobsProvider`/`useJobsStore()`, mounted innermost of the three shell-level providers, inside `InboxProvider`/`TasksProvider`) — active-jobs list, SSE `job_updated`-nudged + 15s-polled (poll runs only while any job is non-terminal), bucket-creation-done transition diff triggers `buckets.refresh()`; new `jobs/JobsChip.tsx` (header indicator, hidden at zero active jobs, spinner + blue `needs_user` dot) + `jobs/JobsPanel.tsx` (right slide-over, per-`(kind,stage)` cards/actions); `AppShell`'s header moves inside the provider nesting so `JobsChip` can read the store, gains `reviewJob` state; `NewTaskWizard` **restructured** to two user-initiated moments — start (goal → `startCreation` → immediate close) and review (`reviewJob` prop seeded from a `draft_ready` job's payload → `confirmJob` → immediate close) — no more pending/creating steps, no SSE subscription, no auto-navigate (the panel's done-card links instead); `lib/api.ts` gains the jobs fetcher/type set (nested `payload` shape, mirroring the server), deletes the old draft fetchers/types entirely; `lib/sse.ts`'s `task_draft_ready` replaced by `job_updated`; `SchemaEditor` pipeline stages gain ordered `→`-arrow chips with drag-to-reorder + ◀/▶ keyboard fallback (focus follows the moved chip); `ViewBucketsModal`'s delete-confirm moves to render once under the section header. **Phase 5 (actions, spec 006)**: `lib/api.ts` gains the rules/actions type+fetcher set (`ActionRule`, `TaskAction`, `FeedItem = EventFeedItem | ActionFeedItem`, `createRule`/`listRules`/`patchRule`/`deleteRule`/`approveAction`/`rejectAction`/`undoAction`) and `Me.has_write_scopes`; `lib/sse.ts` gains `action_updated {task_id}`; new `client/src/actions/ActionCard.tsx` (shared `ActionCard`/`ActionActivityLine`, used by both the HUD tray/ticker and the task page's own panel); new `pages/task/RulesSection.tsx` (self-owned fetch, no `TasksProvider` involvement) and `pages/task/TaskActionsPanel.tsx` (self-owned fetch, task_id-filtered `action_updated` refetch, reset-on-taskId effect) — both gated tracker-only at their `TaskDetail.tsx` call sites, not internally; `ReviewTray`/`ActivityTicker` now render a mixed `event|action` feed and refetch on ANY unfiltered `action_updated`; `HudPage` gains a session-dismissable scope-migration banner (`!has_write_scopes` → link to `/auth/login`). Full detail → ACTIONS_INDEX.md. |
| CICD_INDEX.md | 695e2f3 (main) | 2026-05-29 | Git pre-commit reference-integrity automation (not HTTP/Celery): .githooks/pre-commit + cicd/scripts/{reference-check,load-env}.sh, scripts/install-hooks.sh symlink into .git/hooks, fuel-code global core.hooksPath forwarder (~/.fuel-code/git-hooks) dispatch + recursion guard, staged-diff vs MANIFEST File/Scope triage via OpenRouter (OpenAI-compatible chat/completions, model anthropic/claude-haiku-4-5), OPENROUTER_API_KEY from gitignored .env, warning-only/fails-open (hook swallows the exit code so --strict never blocks), writes cicd/tasks/update-reference-*.md, doc-stamp contract. |

## Authoring

- New subsystem index → `reference/prompts/CREATE_INDEX.md`
- Extend / split an existing index → `reference/prompts/ADD_REFERENCE.md`

Every doc produced by these prompts MUST carry a top-of-file stamp
`<!-- stamp: <short-sha> (<branch>) | <YYYY-MM-DD> -->` and a matching row here.
If a doc references code that isn't committed yet, commit that code first, then
stamp — an unstamped doc cannot be judged for staleness and is a defect.

The system-wide companion to this corpus is `ARCHITECTURE.md` at the repo root
(processes, inter-process edges, code DAGs, environments, external deps). Use it
as the fallback whenever a needed subsystem index does not exist yet.
