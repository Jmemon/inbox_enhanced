<!-- stamp: d90296a (feature/phase4-bucket-unification) | 2026-07-08 -->

# Client Index

> Scope: React 19 + Vite browser SPA (client/src) — react-router-dom routing (`Gate`→`BrowserRouter`, `AppShell` layout route, `/` HudPage, `/inbox` InboxPage, `/tasks/:taskId` TaskDetail), App/AuthProvider, lib/api fetch wrappers (+ `searchInbox`, `getSyncStatus`, Phase 2B's full task-engine fetcher set, and (**Phase 3**) `getReviews`/`getActivity` + the `FeedItem` type) + lib/sse EventSource singleton (now also carrying the three task SSE events), `state/InboxProvider.tsx` — a shell-level context (`useInboxStore()`) composing `useBuckets`/`useInbox`/`useInboxSse` **once** for the life of the authed session so `/` and `/inbox` read and write one live store instead of each route remounting its own copy (its permanent `useInboxSse` subscription also keeps the SSE singleton pinned open, superseding the old `AppShell` no-op pin), `state/TasksProvider.tsx` (**new**, Phase 2B) — the analogous shell-level task store (`useTasksStore()`), nested inside `InboxProvider` in `AppShell`, driving both the HUD's task cards and `/tasks/:taskId`'s detail page off one shared list+per-task-detail cache and one SSE subscription, shared debounced inbox search (`pages/search/`) used by both inbox-facing routes, HudPage's store-derived bucket counts + sync-freshness strip + task cards with a **(Phase 3)** per-card stage histogram + new-task entry point + **(Phase 3)** a task-first layout (sync strip → Tasks → `ReviewTray` → `ActivityTicker` → "Recently active" inbox list → Buckets), inbox list + pagination + reload, bucket filter/new/view modals, and the full task-engine UI (`pages/task/`: `NewTaskWizard`+`SchemaEditor` goal→draft→create flow, `TaskDetail` composition root, `PipelineBoard`, `EntityDrawer`, `ReviewFeed`, `ThreadsPanel`, and (**Phase 3**) the shared `pendingReasons.ts` helper `ReviewFeed` and the HUD's `ReviewTray` both read) plus (**Phase 3**, new HUD-level components) `pages/hud/ReviewTray.tsx` (aggregated cross-task review queue) and `pages/hud/ActivityTicker.tsx` (aggregated cross-task activity feed) — both self-owned-data components backed by the new `/api/reviews`/`/api/activity` routes, independent of `TasksProvider`'s own cache; the Browser↔API fetch+SSE realtime layer (§1.6, §2.1, §2.11, §3.4).
>
> **Phase 4 additions** (bucket unification — buckets are now `tasks(kind='bucket')` server-side, see TASKS_INDEX): `useBuckets()` (`pages/buckets/useBuckets.tsx`) is now **task-backed** — its `refresh` calls `getTasks({kind:'bucket'})` (`GET /api/tasks?kind=bucket`) instead of a dedicated bucket-list fetch, and maps each `Task` row into the same `Bucket` shape (`id`/`name`/`criteria`/`is_default`) every consumer already expected; `rename`/`softDelete` now call `patchTask`/`deleteTask` instead of bucket-specific fetchers, and its own `create` is **deleted** (bucket creation moved to the task wizard — see next). `lib/api.ts`'s dedicated bucket fetchers (`createBucket`, `patchBucket`, `deleteBucket`, `postBucketDraftPreview`, `getBucketDraftPreview`) and `lib/sse.ts`'s `bucket_draft_preview` `SseDataEvent` member are all **deleted** — `Bucket`/`BucketExampleIn` survive as pure client-side types (the mapped shape of a `GET /api/tasks?kind=bucket` list item; `BucketExampleIn` reused as `createTask`'s example element). `pages/buckets/NewBucketModal.tsx` is **deleted outright** — bucket creation now goes through `pages/task/NewTaskWizard.tsx`, which gains a `kind?: 'tracker' | 'bucket'` prop (default `'tracker'`): bucket mode skips the `<SchemaEditor>` step entirely (`state_schema` always sent as `null`) and takes an `onCreated?: (taskId) => void` callback in place of tracker mode's post-backfill `navigate(/tasks/{id})` (there's no per-bucket detail page to navigate to). `InboxPage.tsx` renders `<NewTaskWizard kind="bucket" onCreated={() => buckets.refresh()} onClose={...}>` where `NewBucketModal` used to render; its own `createWithWatchdog` (the 60s+150s reclassify-completion watchdog) is **deleted** — bucket backfill's `threads_updated` publish is deterministic on completion (unlike the old fire-and-forget `reclassify_user_inbox` it replaced, see INBOX_SYNC_INDEX), so nothing needs to compensate for a missed SSE event anymore.

## Files
| Path | Role / key exports (components, hooks) |
|------|----------------------------------------|
| client/src/main.tsx | Entry. `ReactDOM.createRoot(#root)` renders `<App>` in `<React.StrictMode>`. |
| client/src/App.tsx | `App` (default) wraps `<AuthProvider>`; inner `Gate` (renamed from `Routes` — react-router-dom owns that name now) switches on `useAuth().state.status`: `loading→<Splash>`, `anon→<Login>`, `authed→<BrowserRouter><Routes>` — see router table below. **Updated** (Phase 2B): adds the `/tasks/:taskId` leaf route (`TaskDetail`, `pages/task/TaskDetail.tsx`) alongside `/` and `/inbox`, still under the one `<AppShell>` layout route. |
| client/src/auth/useAuth.tsx | `AuthProvider`, `useAuth()`, types `Me`/`AuthState`. Context state `loading|authed|anon`. Shared `fetchMe()` helper (`getJSON('/auth/me')`) backs both `refresh()` (mount-time; any failure → `anon` — correct there since a failed initial load has no session to preserve) and `recheckSession()` (**new** — `AppShell`'s SSE error-streak escape hatch; flips to `anon` only when the caught error is `{kind:'unauthorized'}`, i.e. a definitive 401 from `lib/api.ts`, leaving state untouched on any other failure shape — a backend outage must not force-logout an otherwise-valid session). `signOut()`→`postEmpty('/auth/logout')`. `useEffect` calls `refresh()` on mount. |
| client/src/auth/Login.tsx | `Login` (default). Static card; button → `window.location.assign('/auth/login')`. Reads `?authError` from URL (`denied`→"sign-in cancelled"). |
| client/src/auth/Splash.tsx | `Splash` (default). Static "loading…" screen for the `loading` auth state. |
| client/src/AppShell.tsx | `AppShell` (named export). react-router layout route element — rendered once for the authed session, wraps `/`, `/inbox`, and (**new**, Phase 2B) `/tasks/:taskId` via `<Outlet>`, itself wrapped in `<InboxProvider>` (the old no-op SSE-pinning `useEffect` is gone; the provider's own `useInboxSse` subscription pins the singleton open structurally). `AppShell` also holds its own `subscribeSse` call for a different purpose: an error-streak counter (`consecutiveErrorsRef`, reset on `_open`, incremented on `_error`) that, at ≥3 consecutive `_error`s with no intervening `_open` and throttled to once per 30s, calls `useAuth().recheckSession()` — the auth-death escape hatch for a session whose cookie has expired (nothing about a dead SSE stream alone distinguishes "connection down" from "session dead"; see gotchas for the 401-only semantics). Because `AppShell` and `InboxProvider` mount/unmount in the same React commit, this second handler doesn't reintroduce the handler-count-transiently-zero churn the old no-op pin existed to prevent. **Updated** (Phase 2B): `<InboxProvider>` now also wraps a nested `<TasksProvider>` (outer/inner — both permanent, session-lifetime providers, so the nesting order has no remount implications, just render-after-order) around the same `<Outlet>`, so every route below (HUD, inbox, and the new task-detail page) can call `useTasksStore()`. Renders the persistent header: title, `NavLink`s to `/` (HUD) and `/inbox` (Inbox), signed-in user email, sign-out button. Returns `null` unless `state.status === 'authed'`. |
| client/src/state/InboxProvider.tsx | `InboxProvider`/`useInboxStore()` (**new**). Context provider mounted once by `AppShell`, wrapping `<Outlet>` — the single shared inbox store for the authed session. Owns, in order: `pathname = useLocation()` (first, for stable hook order), `buckets = useBuckets()`, `filterSelection` state, `inbox = useInbox({buckets: buckets.buckets, filterSelection, autoExtendEnabled: pathname==='/inbox'})` — **updated**: gates auto-extend to the `/inbox` route so the session-wide instance doesn't fire a Gmail history-extend at login/on the HUD — then a permanent `useInboxSse({onApply: inbox.applyThreadUpdates, snapshot: inbox.snapshot})` subscription. Context value `{buckets, inbox, filterSelection, setFilterSelection}`, memoized over those four references. `useInboxStore()` throws if called outside the provider. Composes `useBuckets`/`useInbox`/`useInboxSse` — see their own rows below. |
| client/src/state/TasksProvider.tsx | `TasksProvider`/`useTasksStore()` (**new**, Phase 2B). Mounted once by `AppShell`, nested inside `InboxProvider` — the analogous shared task store for the life of the authed session (same rationale as `InboxProvider`: survive route navigation between the HUD and `/tasks/:taskId` instead of remounting/re-subscribing SSE per route). State: `tasks: Task[]` (list, no `state_schema`) + `byId` (memoized), `details: Record<id, TaskDetail>` (per-task cache, populated lazily by `loadDetail`), `backfill: Record<id, {scanned, matched, done}>` (live progress, from `task_backfill_progress`). `refresh()` → `GET /api/tasks`. `loadDetail(id)` → `GET /api/tasks/{id}`; on a 404 (detected via `e instanceof ApiError && e.status === 404` — `getJSON` throws a structured `ApiError` carrying the real `.status` for any non-401 non-ok response, replacing the old `Error.message.includes('404')` substring match a status-echoing proxy could have false-positived) evicts the id from `details` and triggers a list `refresh()`, returning `null` rather than rethrowing (the DELETE case); any other failure logs + rethrows. `createTask`/`patchTask`/`deleteTask` wrap their `lib/api.ts` calls, updating `details`/`tasks` optimistically off the response then always calling `refresh()`. `taskFromDetail(detail)` strips `state_schema` back off a `TaskDetail` to produce the list-shaped `Task` — so one `GET .../{id}` response satisfies both the detail cache AND keeps the `tasks` list item current, no second fetch. One persistent `subscribeSse` effect (mount-once, stable deps): `_open` → `refresh()` (reconnect catch-up — `task_updated`s published while the stream was down are otherwise lost); `task_updated` → looked up in `byIdRef.current` (a ref synced from `byId` via its own effect, read instead of the state directly so this long-lived closure sees the CURRENT list, not what it captured at mount — the same stale-closure trap `useInbox.tsx`'s `lastInternalDate` ref avoids); unknown `task_id` (created elsewhere) → full `refresh()`; known → `loadDetail(task_id)` **only if** `e.version > known.version OR e.pending_count !== known.summary.pending_reviews` (see Gotchas — version alone is insufficient, per TASKS_INDEX's no-bump-on-reject/attach/detach/criteria-mutation contract); `task_backfill_progress` → updates `backfill[task_id]`, plus `loadDetail(task_id)` when `done`. A separate mount-time `useEffect(() => { void refresh() }, [])` exists BECAUSE `_open` only fires for handlers subscribed before the `EventSource` connects — `TasksProvider` mounts inside `InboxProvider`, which has usually already consumed the singleton's first `_open` by the time this effect runs, so without this explicit fetch the task list would stay empty until the next reconnect. |
| client/src/lib/api.ts | Fetch wrappers + types. Exports **`ApiError`** class (**new** — `extends Error`, `public readonly status: number`) — thrown by `getJSON` for any non-401 non-ok response (message `` `${status} ${statusText}` ``, real status preserved) and by `throwWithDetail` (below); deliberately NOT used for 401s, which stay the plain-object `{kind:'unauthorized'}` shape `useAuth`/`recheckSession` key off of instanceof-free. `getThreadsBatch` still throws a bare `Error` for a non-ok response, unchanged — no `.status` on it. Exports `getJSON`/`postEmpty` (401→`throw {kind:'unauthorized'}`), `getInbox`, `searchInbox(q)` (`GET /api/search?q=`→`InboxPage` shape, same as `getInbox`; no console timing wrapper unlike the other calls), `getThread`, `getThreadsBatch`, `requestRefresh`, `postInboxExtend`, `getSyncStatus()` (`GET /api/sync/status`, also uninstrumented like `searchInbox`) — feeds `HudPage`'s freshness strip. **Phase 4: `getBuckets`/`createBucket`/`patchBucket`/`deleteBucket`/`postBucketDraftPreview`/`getBucketDraftPreview` are all deleted** — buckets are now `tasks(kind='bucket')` server-side, so `useBuckets` reads/writes through the task fetchers below (`getTasks({kind:'bucket'})`/`patchTask`/`deleteTask`) instead; `Bucket`/`BucketExampleIn` survive as pure client-side types — `Bucket` is now documented as "the mapped shape of a `GET /api/tasks?kind=bucket` list item," not a dedicated response type. Types `InboxMessage` (`is_unread?: boolean`) / `InboxThread` (`is_archived: boolean`) / `InboxPage`/`Bucket`/`BucketExampleIn`/`AuthError`/`SyncStatus` (`{last_synced_at: number\|null; has_cursor: boolean}`). All `credentials:'same-origin'`; verbose `console.log` timing (`searchInbox`/`getSyncStatus` excepted). **New** (Phase 2B) — the full task-engine fetcher set, mirroring `api/tasks.py`'s `_serialize_*` shapes field-for-field: types `Task` (**Phase 4**: gains optional `criteria?`/`is_default?` — the kind-conditional extras `_serialize_task_list_item` only adds for `kind='bucket'` rows)/`TaskSummary`/`TaskStateSchema`/`TaskDetail` (`Task & {state_schema, criteria: string}`)/`TaskEntity`/`TaskEvent` (incl. `pending_reason`/`proposed_entity`)/`TaskDraftProposal`/`TaskDraftPoll` (`pending`\|`gone`\|`ready`). **Updated** (Phase 3): `TaskSummary` gains `stage_counts: Record<string, number>` — a type comment notes the key order is server-controlled (schema stage order, then observed extras, then `"(no stage)"` last) and consumers must preserve it via plain `Object.entries`, never sort. **New** (Phase 3): `FeedItem = TaskEvent & {task_id, task_name, entity_display_name: string | null}` — the cross-task feed item both aggregate routes return, mirroring `_serialize_feed_event`. Calls: `postTaskDraft(goal)` (202→`{draft_id}`), `getTaskDraft(draftId)` (200/202/404 poll-fallback mapping; a 403 "not your draft" falls through to the generic throw), `createTask(body)` (**Phase 4**: `body` gains an optional `kind?: 'tracker' | 'bucket'`, forwarded straight through to `POST /api/tasks`; 201s the full `TaskDetail`, not the bare `Task`), `getTasks(opts?: {kind?: 'bucket'})` (**Phase 4**: `kind: 'bucket'` → `GET /api/tasks?kind=bucket`, the task-backed replacement for the deleted `getBuckets`; omitted → unchanged tracker-only call, the HUD grid's call sites need no changes), `getTask(id)`, `patchTask(id, body)`, `deleteTask(id)` (204) — both now also `useBuckets`' `rename`/`softDelete` substrate, not tracker-only, `getTaskBoard(id)`, `getTaskEvents(id, {status?, entity_id?, page?, limit?})` (query-param builder, all optional), `getTaskThreads(id)`, `attachThread(id, threadId, addExample)` (`POST .../threads {thread_id, add_example}`, 201 body discarded — fire-and-forget, the `task_updated` SSE push + caller refetch is the source of truth), `detachThread(id, threadId, addExample)` (`DELETE .../threads/{threadId}?add_example=`, 204), `approveEvent`/`rejectEvent`/`revertEvent(id, eventId)` (200, bodies discarded), `setEntityState(id, entityId, field, value)`, `mergeEntity(id, entityId, intoEntityId)`. **New** (Phase 3): `getReviews(limit?)` (`GET /api/reviews?limit=` → unwraps `{reviews: FeedItem[]}`) and `getActivity(limit?)` (`GET /api/activity?limit=` → unwraps `{activity: FeedItem[]}`) — both plain synchronous GETs, no SSE/poll dance; `limit` omitted entirely (not even `?limit=`) when the caller passes none, relying on the server's own default. **`throwWithDetail(r, fallback)` helper** — the client-side half of the server's 422-detail contract (see TASKS_INDEX): on a non-ok response, tries to parse the JSON body and use a **string** `detail` field as the thrown **`ApiError`**'s message (status preserved), falling back to a generic `"{action}: {status}"` message if the body isn't JSON or `detail` isn't a string (e.g. a pydantic list-shaped validation error) — used by `createTask`, `setEntityState`, and `mergeEntity`, the three routes whose 422s carry a meaningful hand-written message (`state_schema`/`coerce_value`/self-merge failures) worth surfacing verbatim in the UI. `getJSON`'s own non-401 error path and `throwWithDetail` both throw the same `ApiError` class, so `TasksProvider`'s `.status === 404` check (see its own row) works uniformly across any `getJSON`-backed call, not just these three. |
| client/src/lib/sse.ts | EventSource singleton. Exports `subscribeSse(handler)→unsub`, types `PreviewExample`/`SseDataEvent`/`SseConnEvent`/`SseEvent`. One `_es=EventSource('/api/sse',{withCredentials:true})`; `_handlers:Set`; closes when the last handler unsubscribes. On error, broadcasts synthetic `_error`, closes `_es`, then — if handlers remain — reopens via `setTimeout` with exponential backoff (`1000 * 2**(consecutiveErrors-1)` ms, capped at 30s; `_consecutiveErrors` resets to 0 on `onopen`), replacing the old immediate `queueMicrotask(_open)` reopen (which hammered an expired session's 401s with no backoff at all). `_reconnectTimer` is nulled at the top of its own callback (before calling `_open()`) and cleared by `_close()` (last-unsubscribe or explicit close), so a pending backoff reopen can't resurrect the stream after sign-out. The permanent handler that keeps `_handlers` non-empty for the whole authed session is `InboxProvider`'s own `useInboxSse` subscription (mounted once by `AppShell`); `AppShell` also holds a second, independent handler purely for auth-recheck error-streak counting — see CLIENT gotchas. **Updated** (Phase 2B): `SseDataEvent` gains the three task-engine events — `task_draft_ready {draft_id}`, `task_updated {task_id, version, pending_count}`, `task_backfill_progress {task_id, scanned, matched, done}` — same union, same one `EventSource`. **Phase 4: the `bucket_draft_preview` member is deleted** (along with the bucket-draft-preview flow it fed) — `TasksProvider` is now the third permanent-ish consumer (mount-once effect) alongside `InboxProvider`'s `useInboxSse` and `AppShell`'s error-streak counter; `NewBucketModal`, the fourth former consumer, is gone (its `bucket_draft_preview` subscription went with it) — `NewTaskWizard` (both kinds) subscribes for `task_draft_ready` instead, the same event tracker-mode drafts already used. |
| client/src/pages/hud/HudPage.tsx | `HudPage` (default). Route `/`, the default landing page. No longer fetches its own inbox snapshot: reads `{buckets, inbox} = useInboxStore()` and the shared `useInboxSearch()`. When not searching, renders (**Phase 3**: task-first order) the sync-freshness strip (`agoLabel(status.last_synced_at)`, plus "first sync pending" when `!status.has_cursor`, from HudPage's own `status` state) → a Tasks section → `<ReviewTray>` → `<ActivityTicker>` → a "Recently active" list (renamed from "Recently processed", same `useMemo` over `inbox.idLayer.slice(0, RECENT_COUNT=10).map(id => inbox.displayLayer[id]).filter(Boolean)` via `<InboxList>` — unchanged data, moved position) → a bucket-count grid (`bucketCounts` — `useMemo` tally over `inbox.idLayer.map(id => inbox.displayLayer[id])` by `bucket_id`, captioned "of latest {inbox.idLayer.length}"). While searching, swaps to `<InboxList threads={search.results}>` like `InboxPage`. HudPage's own effect only drives sync-status machinery: initial `getSyncStatus()` fetch, a 30s `refreshStatus` poll, and a cosmetic 5s `setInterval` tick (`forceTick`) that only re-renders `agoLabel` (no refetch) — no SSE handler, no debounce timer; the inbox list itself updates live via the shared store. **Phase 2B additions:** reads `{tasks, backfill} = useTasksStore()`; the Tasks section renders one card per task (name, `statusChipStyle`-colored active/paused chip — a local copy of `TaskDetail.tsx`'s helper, not shared, per this task's scope being HudPage-only — entity count, **(Phase 3)** a `stageHistogramLabel(task.summary.stage_counts)` line (below the entity count, above the pending badge) — a compact `"applied 4 · screen 2 · onsite 1"` string preserving `stage_counts`' server-controlled insertion order (plain `Object.entries`, never sorted), truncated to `STAGE_HISTOGRAM_MAX=4` stages with a trailing `"+n more"`, `null`/omitted entirely when `stage_counts` is empty or absent (guards an older cached payload or an SSE-race snapshot predating the server sending it, even though the type is non-optional) — a `pending_reviews > 0` "N to review" badge, `lastEventAgo(task.summary.last_event_at)` — a one-off ISO-string→`agoLabel` adapter, since `last_event_at` is an ISO string but `agoLabel` otherwise takes epoch seconds like `status.last_synced_at` — and an in-progress `taskBackfill` line when `backfill[task.id]` exists and isn't `done`) plus a trailing "+ New task" tile; clicking a card `navigate`s to `/tasks/{id}`, clicking "+ New task" opens `<NewTaskWizard onClose={...}>` (local `showWizard` state) as a page-level modal, same pattern as `InboxPage`'s bucket modals. The task-card `<div>` also carries `role="button"`/`tabIndex={0}`/`onKeyDown` (Enter/Space → the same `navigate(/tasks/{id})`, `preventDefault` on Space so it doesn't also scroll the page) so the card is keyboard/screen-reader reachable, not just mouse-clickable. `agoLabel` (the sync-freshness strip's helper, reused by `lastEventAgo` for these cards) guards `Number.isNaN(epochSecs)` alongside its pre-existing `=== null` check, returning `'never'` for either: `lastEventAgo` feeds it `Math.floor(Date.parse(iso)/1000)`, which is `NaN` for an unparseable `last_event_at` string and, without the guard, silently fell through every `s < 60`/`s < 3600` comparison (all `false` on `NaN`) to the final `${NaN}h ago` branch. |
| client/src/pages/hud/ReviewTray.tsx | `ReviewTray()` (**new**, Phase 3, no props). Aggregated cross-task review queue on the HUD. **Self-owned data**: fetches `getReviews()` itself on mount rather than reading from `TasksProvider` — the unified feed has no other consumer, so there's nothing to share a cache with. Renders "Needs review (N)" with one card per `FeedItem`: a `Link` to `/tasks/{item.task_id}` (the owning task's name), `EntityLabel` (local variant of `ReviewFeed`'s — prefers `entity_display_name`, falls back to `proposed_entity` + a "new entity?" tag, then "unknown" — built on `FeedItem`'s already-resolved name rather than an `entitiesById` lookup, since there is no single task's entity map here), field/old→new, an `EvidenceQuote` blockquote when present, `pendingReasonCopy(item)` (imported from `../task/pendingReasons`), and approve/reject buttons wired to the same `approveEvent`/`rejectEvent` `TaskDetail` uses (keyed by `item.task_id`), each followed by a direct `refetch()` of this tray — it never touches `TasksProvider`'s store; that store's own SSE-driven `task_updated` convergence is what keeps the HUD's task cards/badges in sync independently. **Refetch triggers**: mount, plus a `totalPending = sum(tasks[].summary.pending_reviews)` `useMemo` from `useTasksStore()` — an early-warning cross-task signal (an approve/reject from inside a task's own `ReviewFeed`, or a backfill/sync landing a new pending event, moves some task's `pending_reviews` via SSE well before this component's own action would notice) — an effect keyed on `totalPending` alone refetches, skipping the initial mount via a `mountedRef` guard (the mount effect already covers first fetch). **Busy-state UX**: same `busy: Set<eventId>` + `BUSY_TIMEOUT_MS=10_000` per-id timeout-fallback shape as `ReviewFeed`'s, independently implemented (not shared) — see that file's Gotchas entry for the underlying race. Empty state renders "Nothing needs review". |
| client/src/pages/hud/ActivityTicker.tsx | `ActivityTicker()` (**new**, Phase 3, no props). Aggregated cross-task activity feed on the HUD, below `ReviewTray`. **Self-owned data**, same pattern as `ReviewTray`: fetches `getActivity()` itself on mount. Renders a compact one-line-per-item list — `OriginBadge` ("LLM"/"you", colored, mirrors `ReviewFeed`'s origin badge verbatim but duplicated rather than shared, per this codebase's existing small-widget-duplication convention), then `{task_name}: {entityLabel} {field} → {new_value} · {createdAgo}`, each line a `Link` to `/tasks/{item.task_id}`. `entityLabel` collapses the same `entity_display_name → proposed_entity → "unknown"` fallback chain to a plain string (no "new entity?" annotation — no room in a one-line ticker item). `agoLabel`/`createdAgo` duplicate `HudPage`'s epoch-seconds/ISO-adapter pair locally (HudPage doesn't export them). **Refetch triggers** (see Gotchas for why two, not one): a `totalVersion = sum(tasks[].version)` signal (catches applies/edits/approves, which all bump `task.version`) AND a separate `totalPending = sum(tasks[].summary.pending_reviews)` signal (catches rejects, which are version-exempt by server design — see TASKS_INDEX) — kept as two independent `useEffect` deps rather than one combined arithmetic sum, because an approve's `+1` version and `-1` pending would otherwise net to zero and silently skip a needed refetch. Same skip-initial-mount `mountedRef` guard as `ReviewTray`. Empty state renders "No recent activity". |
| client/src/pages/inbox/InboxPage.tsx | `InboxPage` (default). Route `/inbox`. **Updated** — no longer wires its own `useBuckets`/`useInbox`/`useInboxSse`: reads `{buckets, inbox, filterSelection, setFilterSelection} = useInboxStore()` (destructuring `buckets.buckets`/`buckets.byId`/`buckets.rename`/`buckets.softDelete` where needed — **Phase 4**: `buckets.create` no longer exists). Owns only local UI state: `showView`/`showNew` modal flags, `hydrateCurrentPage` on page change. **Phase 4**: `showNew` now renders `<NewTaskWizard kind="bucket" onCreated={() => buckets.refresh()} onClose={...}>` where `<NewBucketModal>` used to render (`NewBucketModal.tsx` is deleted); the old `createWithWatchdog` + 60s/150s reclassify watchdog are both **deleted** — bucket backfill's `threads_updated` publish is deterministic on completion, unlike the fire-and-forget `reclassify_user_inbox` this replaced (see INBOX_SYNC_INDEX), so there's nothing left for a watchdog to compensate for. Search is the shared `useInboxSearch()` hook (`pages/search/`). Renders `<SecondaryHeader>`, `<SearchBar>`, `<InboxList>`, modals — no longer renders the page header/nav (moved to `AppShell`). Because `inbox` now lives in `InboxProvider`, `inbox.loading` only flashes "loading…" on the first-ever mount per authed session, not on every navigation back to `/inbox`. |
| client/src/pages/search/useInboxSearch.ts | `useInboxSearch()` (**new**, extracted from the old `Home.tsx` inline state). Owns `query`/`results`/`error` + `searchSeq` ref (monotonic request token). 300ms-debounced `useEffect`→`searchInbox(q)`; discards a response if `seq !== searchSeq.current` (stale/out-of-order); clearing the query resets `results`/`error` to `null` without a network call. Consumed by both `HudPage` and `InboxPage`. |
| client/src/pages/search/SearchBar.tsx | `SearchBar({query, setQuery, results})` (**new**). Pure controlled `<input>` + result-count/"clear" button pair; renders nothing extra while `results === null`. Shared by `HudPage` and `InboxPage`. |
| client/src/pages/inbox/useInbox.tsx | `useInbox({buckets,filterSelection,autoExtendEnabled=true})`. Owns inbox id/display layers, pagination, auto-extend, LWW gate. Instantiated **once**, by `InboxProvider`, not per-page. **Updated** — new opt-in `autoExtendEnabled` (default `true`, keeps the hook self-contained for any other caller): the auto-extend effect's first guard is now `if (opts.autoExtendEnabled===false) return`, dep array includes it; `InboxProvider` passes `pathname==='/inbox'` so the session-wide instance only extends while the inbox route is actually mounted. `applyThreadUpdates` partitions LWW-accepted threads into **archived** (evicted outright from `idLayer`/`displayLayer`/`lastInternalDate` — mirrors a Gmail archive) vs **live** (merged + re-sorted as before). (details below). `PAGE_SIZE=50`, `SNAPSHOT_LIMIT=200`, `EXTEND_TIMEOUT_MS=90_000`, `UNCLASSIFIED='unclassified'`. |
| client/src/pages/inbox/useInboxSse.tsx | `useInboxSse({onApply,snapshot})`. Subscribes SSE. **Updated** — instantiated **once**, by `InboxProvider` (superseding the old per-`InboxPage` mount and the old `AppShell` no-op pin); unmodified internally. On `_open` runs snapshot→buffer-drain lifecycle; buffers `threads_updated` until `ready`; on `_error` clears `ready`. |
| client/src/pages/inbox/InboxList.tsx | `InboxList({threads,bucketsById,emptyLabel})`. Grid rows (bucket pill, from, subject, preview). Helpers `abbreviate`, `BucketPill`. **Updated**: `emptyLabel?: string` prop (default `'syncing your inbox…'`) renders in place of the list when `threads.length===0`; search call sites (`HudPage`, `InboxPage`) pass `emptyLabel="no results"` so an empty search doesn't misleadingly read as "inbox hasn't synced yet". |
| client/src/pages/inbox/Pagination.tsx | `Pagination({page,pageCount,extending,onChange})`. prev/next + "page N of M" + "loading more…" when extending. Returns null when `pageCount<=1 && !extending`. |
| client/src/pages/inbox/ReloadButton.tsx | `ReloadButton({onResync})`. Click fires `requestRefresh()` (202 kick) AND `onResync()` in parallel; ~1.5s "syncing…" busy state. |
| client/src/pages/buckets/useBuckets.tsx | `useBuckets()`. State `buckets`/`loading`. **Phase 4: task-backed** — `refresh()` calls `getTasks({kind:'bucket'})` (`GET /api/tasks?kind=bucket`, the task-backed replacement for the deleted `getBuckets`) and maps each `Task` row into the `Bucket` shape (`id`/`name`/`criteria: t.criteria ?? ''`/`is_default: t.is_default ?? false` — the `??` fallbacks guard a hypothetical non-bucket row shape, though every row this call returns is already `kind='bucket'`). Exports `buckets`, `byId`, `customBuckets`, `refresh`, `rename`/`softDelete` (now call `patchTask`/`deleteTask` instead of bucket-specific fetchers, each still api-then-`refresh()`). **`create` is deleted** — bucket creation moved to `NewTaskWizard kind="bucket"` (`InboxPage.tsx`), which calls `useTasksStore().createTask` directly rather than through this hook. Instantiated **once**, by `InboxProvider`, not per-page; otherwise unmodified. |
| client/src/pages/buckets/FilterByBucketDropdown.tsx | `FilterByBucketDropdown({buckets,selection,onChange})`. Multiselect incl. `UNCLASSIFIED`; `selection===null` means "all". Emits `null` when all selected. |
| client/src/pages/buckets/SecondaryHeader.tsx | `SecondaryHeader(...)`. Toolbar: `<ReloadButton>`, `<FilterByBucketDropdown>`, view/new bucket buttons, right-aligned `<Pagination>`. Pure prop pass-through from `InboxPage` (renamed from `Home`). |
| client/src/pages/buckets/NewBucketModal.tsx | **Deleted (Phase 4).** Used to own the `form\|pending\|review` bucket-draft-preview step machine (`draftId`, `examples`, `appliedRef` idempotency, SSE + 5s poll resolution). Bucket creation now goes through `pages/task/NewTaskWizard.tsx`'s `kind="bucket"` mode instead — see that file's row and the Phase 4 scope note above for exactly what carried over vs. changed. |
| client/src/pages/buckets/ViewBucketsModal.tsx | `ViewBucketsModal({buckets,onClose,onRename,onDelete})`. Inline rename (onBlur), delete with confirm copy (default buckets locked). |
| client/src/pages/task/NewTaskWizard.tsx | `NewTaskWizard({onClose, kind='tracker', onCreated})` (Phase 2B; **Phase 4: gains `kind`/`onCreated`**, superseding the deleted `NewBucketModal.tsx`). Originally cloned from that modal's step machine (same `appliedRef`-per-`draft_id` idempotency, same SSE-fast-path + 5s-poll-fallback dance, same `Backdrop`/modal styles). Steps: `form` (goal textarea) → `pending` ("reading your goal…") → `review` (name/description/`SchemaEditor` + example triage) → `creating` (post-create backfill wait). **Phase 4 bucket mode** (`kind="bucket"`, used only by `InboxPage.tsx`): `applyDraft` discards the draft's proposed `state_schema` and keeps `stateSchema` permanently `null` (the review step's gate becomes `step==='review' && (kind==='bucket' || stateSchema)`, and it skips rendering `<SchemaEditor>` entirely); `submit()` sends `kind` straight through to `createTask`; the post-create completion effect branches on `kind` — tracker mode still `navigate(/tasks/{taskId})` (there's a detail page), bucket mode instead calls `onCreated?.(taskId)` (there's no per-bucket detail page) — both branches still call `onClose()`. Copy also branches on `kind` (modal title "New bucket" vs "new task", submit button "create bucket" vs "create task", goal-step prompt). Every other line is unchanged, byte-identical tracker behavior. Differences from the original bucket-draft-preview modal this superseded (both kinds, inherited from the tracker flow's own history): (1) an extra `creating` step; (2) the SSE payload (`task_draft_ready`) carries only `draft_id`, not the proposal (unlike the deleted `bucket_draft_preview`, which used to inline `positives`/`near_misses` directly) — the fast path is `event → getTaskDraft(id) → applyDraft`; (3) no "more examples" affordance; (4) two inline error paths the old bucket modal didn't have — a `pending`-step draft-expiry dead end (`draftError`, set when the 5s poll fallback observes the redis draft cache has gone `gone` before a result ever arrived; renders the message plus a `back` button wired to `backToForm()`, which clears `draftError`/`draftId` and returns to `form` with the typed `goal` text preserved) and a `form`-step `startError` (set when `startDraft()`'s `postTaskDraft` call throws). All three error banners (`startError`/`draftError`/the pre-existing review-step `createError`) share one `errorBannerStyle` constant. `applyDraft` seeds `name`/`description`/`stateSchema`/`keywordProbes`/`examples` (each example carries `initial` — the server's suggested `positive`/`near_miss` choice — plus a mutable `choice` incl. `rejected`) and flips to `review`, guarded by `appliedRef` so a result arriving via BOTH SSE and poll (either order) only applies once. `submit()` calls `trimStateSchema` (drops empty pipeline-stage chips and empty-key attributes after `.trim()` — avoids a round-trip 422 for whitespace the user never meant to submit, tracker mode only — bucket mode's `stateSchema` is always `null`) then `useTasksStore().createTask(...)`; a 422 or other failure renders `createError` inline and stays on `review` (the store's `createTask` rethrows `apiCreateTask`'s `Error` as-is). On success, sets `taskId` and moves to `creating`; a dedicated effect (keyed on `backfill[taskId]?.done`, guarded by `navigatedRef` so it fires at most once) hands off per `kind` as described above once that task's backfill progress reports `done` — this relies on `TasksProvider`'s `task_backfill_progress` handler recording `backfill[taskId]` unconditionally by id with no check against its (tracker-only) task list, so a bucket task id's progress still lands here even though it's absent from that list. |
| client/src/pages/task/SchemaEditor.tsx | `SchemaEditor({value, onChange})` (**new**, Phase 2B). Purely controlled editor over one `TaskStateSchema`: pipeline `stages`/`terminal` as inline-editable chip lists (`ChipList` — each chip IS a text input, no separate draft buffer; "+ add" appends a blank editable chip), a "singleton task" checkbox that nulls `entity` out while a `lastEntityRef` (synced one-render-behind via effect, same pattern as `TasksProvider`'s `byIdRef`) remembers the last non-null entity so toggling off→on→off round-trips the user's edits instead of resetting to blank, and — when not singleton — entity `noun`/`identity_hint` fields plus a typed attribute table (key/type-select from the fixed `ATTR_TYPES` five-value list mirroring the server's `schema.py::ATTR_TYPES`/comma-separated `values` input shown only for `type==='enum'`, cleared on switching away from enum/seeded to `[]` on switching to it/remove button). No client-side validation beyond that structural shape — the server's `state_schema` validator (`task_engine/schema.py`) is the source of truth and surfaces as a 422 the wizard renders inline. Shared by `NewTaskWizard` (initial create) — `TaskDetail`/`PipelineBoard` render the schema read-only, they don't reuse this editor. |
| client/src/pages/task/TaskDetail.tsx | `TaskDetail` (default). Route `/tasks/:taskId` (**new**, Phase 2B) — the task detail composition root. Reads `{getDetail, loadDetail, patchTask, deleteTask} = useTasksStore()` and `{buckets} = useInboxStore()`. **Data ownership split** (see Gotchas): the cached `detail` (name/status/version/summary/state_schema/criteria) comes from `TasksProvider`'s shared cache (`getDetail(taskId)`, kept fresh by `loadDetail`); `entities`/`events`/`threads` are each owned locally (`useState` + their own `refetchBoard`/`refetchEvents`/`refetchThreads` GETs), refetched independently by every action handler AND all three together (`refetchAll`) whenever `detail.version`/`detail.summary.pending_reviews` changes identity (the SSE-driven refetch cascade — `TasksProvider` already decided a real change happened; this effect keys on exactly those two fields, not `detail` itself, which is a fresh object every `loadDetail` call). `refetchEvents` fetches two parallel windows merged by id: the recent-events window (newest 50, any status, the default limit) AND a wider pending-review-only window (newest 200 `pending_review` events, so the review tray never silently omits a pending event that's counted in `task.summary.pending_reviews`) — both windows are deduped and re-sorted newest-first before rendering. A `taskId` (route param) change resets all three local caches + `selectedEntityId` before re-loading, so navigating task→task without an `AppShell` unmount can't flash the previous task's data. **Drawer state ownership**: `selectedEntityId` lives here, not inside `PipelineBoard` — the board's props are locked to `{schema, entities, onMove, onOpenEntity}` with nowhere to hang drawer events off of, so `EntityDrawer` renders as `PipelineBoard`'s **sibling** (fixed-position overlay, so its place in the grid doesn't affect layout) fed from state `TaskDetail` already owns (`entitiesById`, `events`, `schema`). Action handlers: `handleEditEntity` (async, RE-THROWS — `EntityDrawer` awaits it and renders the 422 message inline) wraps `setEntityState` + refetch(board,events); `handleMove` is its synchronous fire-and-forget `field="stage"` wrapper for the board's locked `onMove` prop; `handleMergeEntity` owns the `window.confirm` (the drawer just calls `onMerge` for whatever target was picked), the API call, closing the drawer, and refetch; `handleApprove`/`handleReject`/`handleRevert`/`handleDetach`/`handleAttach` are all fire-and-forget-then-refetch, failures `console.error`-only (no inline surface — unlike `handleEditEntity`); `handleToggleStatus`/`handleDelete` call `patchTask`/`deleteTask` (delete confirms via `window.confirm`, then navigates to `/`). `sortedThreads` client-sorts `GET .../threads`' unordered response newest-first by `recent_message.internal_date`. |
| client/src/pages/task/PipelineBoard.tsx | `PipelineBoard({schema, entities, onMove, onOpenEntity})` (**new**, Phase 2B — replaces an earlier stub). Columns: pipeline `stages` in schema order, then `terminal` stages (dimmed — "done" states, not somewhere to drag things further), preceded by a leading "(no stage)" column for entities with no stage yet or a stage value that matches no currently-declared column (e.g. renamed out from under existing data by a schema edit) — omitted entirely when empty. Singleton tasks (`schema.entity === null`) always have exactly one entity (`entity_key='_self'`, per `task_engine/schema.py`'s docstring) so the same column layout already renders as a one-card board for them — no special-cased path. Each `EntityCard` shows `display_name`, up to 3 non-stage fields in **schema attribute order** (`nonStageFieldEntries` — not raw `Object.keys()` order, which just reflects JSON serialization order; falls back to the state's own keys for singleton tasks, which have no attribute list to order by; drops attributes the schema declares but this entity hasn't had extracted yet rather than rendering blank), and a "move to ▾" `<select>` (stops click-propagation so interacting with it doesn't also open the drawer via the card's own `onClick`) offering every stage INCLUDING terminal ones directly (you can jump straight to a terminal stage without stepping through interim ones — client renders the option, the server's own step-3 backward/terminal guards still apply and may route it to `pending_review`). Clicking the card body (not the select) calls `onOpenEntity(entity.id)`. |
| client/src/pages/task/EntityDrawer.tsx | `EntityDrawer({taskId, entity, schema, events, onClose, onEdit, onMerge, onRevert, allEntities})` (**new**, Phase 2B). A right-side fixed panel (not a `NewTaskWizard`-style centered `Backdrop` modal — a drawer reads better anchored to an edge next to the board it's inspecting, which stays visible/scrollable behind it) with a full-bleed transparent scrim behind it that calls `onClose` on click (same click-outside-to-close idea as `Backdrop`). Renders one `FieldRow` per schema field: `stage` and any `enum`-typed attribute are `<select>`s; everything else is an **uncontrolled** `<input>` keyed on the field's current true value (`key={entity.state[attr.key]}`) with `defaultValue` — React only remounts (re-seeding `defaultValue`) when the source of truth changes underneath it (e.g. this drawer's own successful commit triggering `TaskDetail`'s refetch), so in-progress typing survives re-renders but a successfully committed value always wins over a stale local edit; commits on `onBlur`. Every field edit funnels through `commit(field, value)`: clears any previous inline `error`, `await`s the caller's `onEdit` (`TaskDetail`'s `handleEditEntity`, which rethrows), and on rejection renders the message inline — the schema validator's 422 detail (e.g. "'x' is not a valid stage") is the whole reason this exists instead of a bare `console.error`. `error`/`mergeTarget` reset via effect keyed on `entity.id` (so a stale 422 from entity A doesn't linger when the user opens entity B). Merge picker (`mergeCandidates` = every other entity) fires `onMerge` directly with no local `confirm()` — `TaskDetail`'s `handleMergeEntity` owns that. **History**: `entityEvents` filters+sorts (newest-first) the FULL `events` array `TaskDetail` passed down, itself down to `entity_id === entity.id` — same "filter the parent's full array" convention `ReviewFeed` uses; see CLIENT gotchas for why this is bounded by the parent's own fetch limit. Each history row shows `field: old → new (origin, status)`, the evidence quote if present, and a `revert` button gated to `status==='applied'`. |
| client/src/pages/task/ReviewFeed.tsx | `ReviewFeed({events, entitiesById, onApprove, onReject, onRevert})` (**new**, Phase 2B). Two lists: "Needs review" (`status==='pending_review'`, unbounded within whatever `events` the parent fetched) and "Recent activity" (everything else, `.slice(0, 30)` — safe to slice-without-resort because `list_task_events` already orders newest-first and filtering preserves that order). **Updated** (Phase 3): `pendingReasonCopy` (the server's 5-value `pending_reason` enum → user-facing copy) MOVED OUT verbatim to `./pendingReasons.ts` (below) — this file now just imports it — so the HUD's `ReviewTray` can share the exact same copy without duplicating the switch statement. `EntityLabel` resolves a display name: the entity's current `display_name` if `entity_id` still resolves in `entitiesById`, else the LLM's raw `proposed_entity` string with a "new entity?" suffix (for a still-pending propose-new-entity event), else "unknown" — still local to this file (`ReviewTray`'s own `EntityLabel` is a variant built on `FeedItem`'s pre-resolved `entity_display_name`, not this component's `entitiesById` lookup, so it isn't shared). **Busy-state UX** (see Gotchas for the underlying race): approve/reject buttons disable per-event-id (`busy: Set<string>`) the instant they're clicked (`markBusy`); the busy flag clears automatically once a REFETCHED `events` snapshot no longer lists that id as `pending_review` (effect keyed on `events` identity, not the derived `pending` array, which is fresh every render) — because `onApprove`/`onReject` are fire-and-forget (no promise to await) and a failed request never refetches, a **10-second timeout fallback** (`BUSY_TIMEOUT_MS`) also clears the busy flag per-id so a failed approve/reject doesn't leave its button stuck disabled forever; whichever clears first (event-driven or timeout) cancels the other via `busyTimersRef`. The HUD's `ReviewTray` (below) copies this exact busy-tracking shape rather than sharing it. |
| client/src/pages/task/pendingReasons.ts | `pendingReasonCopy(event: TaskEvent): string \| null` (**new**, Phase 3 — relocated verbatim from `ReviewFeed.tsx`). Takes the wider `TaskEvent` type (not `FeedItem`) specifically so both `ReviewFeed` (per-task `TaskEvent[]`) and the HUD's `ReviewTray` (cross-task `FeedItem[]`, a `TaskEvent` superset) can import the one function — same switch over the 5-value `pending_reason` enum, same "any other value (incl. `null`) omits the line" fallback, unchanged behavior from its pre-move form. |
| client/src/pages/task/ThreadsPanel.tsx | `ThreadsPanel({taskId, threads, bucketsById, onDetach, onAttach})` (**new**, Phase 2B). Two sections: attached threads (`AttachedRow` — subject/sender/date + bucket name + a per-row "teach the task" checkbox, default checked, matching the API's `add_example` default + a `detach` button) and an "Add emails" search-to-attach box (`useInboxSearch()` + shared `SearchBar`, filtering out threads already in `attachedIds` — attached threads shouldn't also appear as attachable results, per the brief — `SearchResultRow` mirrors `AttachedRow`'s shape but with an `attach` button and inverted checkbox copy: checked means "use as a positive example"). Both row components copy (not import) `InboxList`'s private `abbreviate` sender-formatting helper — reusing `InboxList` itself would drag in its bucket-pill grid/column layout, which this panel's thin actions-column rows don't want. `taskId` is accepted but unused (`/api/search` has no task-scoping param) — kept in the prop contract for a future task-scoped search without another signature change. |

## Routes / Tasks / Entrypoints
- **Routing** (**new**, react-router-dom ^7) — `App.tsx`'s `Gate` renders `<BrowserRouter><Routes>` once authed (`loading`/`anon` still render `<Splash>`/`<Login>` outside the router entirely). One layout route, `<AppShell>`, wraps both leaf routes via `<Outlet>`:

  | Path | Element | Notes |
  |------|---------|-------|
  | `/` | `HudPage` | default landing — snapshot + sync-freshness strip + bucket cards + (**new**, Phase 2B) task cards |
  | `/inbox` | `InboxPage` | full inbox list/search/buckets (ex-`Home`) |
  | `/tasks/:taskId` | `TaskDetail` | **new**, Phase 2B — one task's board/review-feed/threads composition root |
  | `*` | `<Navigate to="/" replace>` | catch-all redirect |

- **AppShell** — the layout route: persistent header (title, `NavLink`s for `/` and `/inbox`, user email, sign-out) wrapping `<Outlet>` in `<InboxProvider>` (the old no-op SSE pin is gone, superseded by the provider's own permanent subscription, see gotchas). `AppShell` also runs its own `subscribeSse` purely to count `_error`/`_open` streaks and trigger `useAuth().recheckSession()` after 3+ consecutive errors (throttled 30s) — see gotchas for the 401-only semantics. **Updated** (Phase 2B): `<InboxProvider>` now also wraps a nested `<TasksProvider>` around the same `<Outlet>`, so `/tasks/:taskId` (and every other route) can call `useTasksStore()`.
- **useAuth** — owns auth context (`loading|authed|anon`). Bootstrap `useEffect`→`/auth/me`. `signOut`→`/auth/logout`.
- **InboxProvider / useInboxStore** (`state/InboxProvider.tsx`) — the shared inbox store, mounted once by `AppShell` for the life of the authed session. Composes `useBuckets`+`useInbox`+`useInboxSse` (all three unmodified) and exposes `{buckets, inbox, filterSelection, setFilterSelection}` via context; `HudPage`, `InboxPage`, and `TaskDetail` (for `buckets` only, to label attached threads' bucket pills) all call `useInboxStore()` instead of instantiating their own hooks. Sign-out still closes the SSE stream: `AppShell` unmounts → `InboxProvider`/`TasksProvider` unmount → each provider's own SSE subscription's last unsub fires → `lib/sse.ts` closes `_es`.
- **TasksProvider / useTasksStore** (**new**, Phase 2B, `state/TasksProvider.tsx`) — the analogous shared task store, mounted once by `AppShell` (nested inside `InboxProvider`) for the life of the authed session. Owns the task list (`tasks`/`byId`), a lazily-populated per-task detail cache (`details`, read via `getDetail`/`loadDetail`), and live backfill progress (`backfill`). One persistent `subscribeSse` subscription drives `refresh()`/`loadDetail()` off `_open`/`task_updated`/`task_backfill_progress` — see its own Files-table row for the full contract, and CLIENT gotchas for the version-gap-alone-is-insufficient refetch rule. `HudPage` reads `{tasks, backfill}`; `TaskDetail` reads `{getDetail, loadDetail, patchTask, deleteTask}`; `NewTaskWizard` reads `{createTask, backfill}`.
- **useBuckets** — **updated**: single shared instance owned by `InboxProvider`, not one per route. `HudPage` reads `buckets.buckets`/`buckets.byId` (bucket-count cards); `InboxPage` additionally uses `buckets.rename`/`softDelete` (each mutates then `refresh()` via `GET /api/tasks?kind=bucket`, **Phase 4**: was `GET /api/buckets`) — bucket **creation** no longer goes through this hook at all (`buckets.create` is deleted; `NewTaskWizard kind="bucket"` calls `useTasksStore().createTask` directly and the wizard's `onCreated` callback triggers `buckets.refresh()` afterward). Because it's one instance, a bucket created/renamed/deleted on `/inbox` is immediately reflected in `HudPage`'s cards without navigating back to remount it.
- **useInbox** — **updated**: single shared instance owned by `InboxProvider` (previously mounted only by `InboxPage`): `idLayer:string[]` (ordered ids) + `displayLayer:Record<id,InboxThread>` (rows), `page`, `more:boolean|null`, `extendInFlight`. Provides `snapshot` (loading toggle), `resync` (silent), `applyThreadUpdates(ids)` (LWW-gates, then partitions accepted threads into archived-evict vs live-merge), `requestExtend`, `hydrateCurrentPage`, derived `pageThreads`/`pageCount`/`filteredIdLayer`. `HudPage` reads `idLayer`/`displayLayer` directly (for its "recently processed" list and bucket counts) but never touches `page`/pagination/filter — those stay `InboxPage`-only concerns even though the underlying hook instance is shared.
- **useInboxSse** — **updated**: single permanent subscription owned by `InboxProvider` (previously mounted only by `InboxPage`, plus a separate `AppShell` no-op pin): `_open`→`snapshot()`, then drains buffered `threads_updated` via `onApply`; live `threads_updated` apply directly once ready. Because this subscription lives for the whole authed session, the inbox stays current even while the user sits on the HUD, not the inbox list.
- **Search** (`pages/search/`, shared location) — `useInboxSearch()`: debounced (300ms) search box used identically by both `HudPage` and `InboxPage`: `query`/`results`/`error` state + `searchSeq` ref (monotonic token, discards stale out-of-order responses) driving `searchInbox(q)`→`GET /api/search`. Non-null `results` replaces the mounted page's `<InboxList>` source; clearing the query (or an empty trimmed value) restores that page's normal view. Independent of `useInbox` — does not touch `idLayer`/`displayLayer`/pagination/filter. Unaffected by the `InboxProvider` refactor.
- **Modals** — `ViewBucketsModal` (rename/soft-delete), shown conditionally from `InboxPage`; `NewTaskWizard` (Phase 2B; form→pending→review→creating goal-to-task flow, `SchemaEditor` nested inside its review step for tracker mode only), shown conditionally from `HudPage` (default `kind="tracker"`) AND, **Phase 4**, from `InboxPage` (`kind="bucket"`, `onCreated` callback) — superseding the deleted `NewBucketModal`; `EntityDrawer` (fixed-position overlay, not a centered backdrop modal), shown conditionally from `TaskDetail`.
- **Key components** — `AppShell` (persistent chrome + provider mount point), `InboxProvider`/`useInboxStore` (shared inbox state), `HudPage` / `InboxPage` (route composition roots, store consumers), `SecondaryHeader` (toolbar), `InboxList`, `Pagination`, `ReloadButton`, `FilterByBucketDropdown`. **New**, Phase 2B: `TasksProvider`/`useTasksStore` (shared task state), `TaskDetail` (route composition root), `PipelineBoard`, `EntityDrawer`, `ReviewFeed`, `ThreadsPanel`, `NewTaskWizard`, `SchemaEditor`. **New**, Phase 3: `ReviewTray`, `ActivityTicker` (both HUD-level, self-owned-data, always rendered — not conditional like the modals above), `pendingReasons.ts`'s `pendingReasonCopy` (shared helper, not a component, imported by both `ReviewFeed` and `ReviewTray`).

## Data & state touched
- **API endpoints via lib/api.ts** (all cookie-auth `same-origin`):
  - `GET /auth/me`, `POST /auth/logout` — useAuth.
  - `GET /api/inbox?page&limit` (`getInbox`, snapshot uses `limit=200`) — useInbox, one shared instance owned by `InboxProvider`.
  - `GET /api/search?q=` (`searchInbox`) — shared `useInboxSearch()` debounced search box (`HudPage` + `InboxPage`); same `InboxPage` response shape as `/api/inbox`, no page/limit args passed by the client.
  - `GET /api/sync/status` (`getSyncStatus`) — `HudPage`'s own `status` state feeds the freshness strip; `{last_synced_at, has_cursor}`. This is the one fetch `HudPage` still owns directly (status-only, not inbox data).
  - `GET /auth/me` (`fetchMe`, shared helper) — also called by `recheckSession()` (**new**), `AppShell`'s SSE-error-streak escape hatch; see gotchas.
  - `POST /api/threads/batch {thread_ids}` (`getThreadsBatch`) — SSE replay / hydrate / extend apply.
  - `GET /api/threads/{id}` (`getThread`) — exported, **no current call site**.
  - `POST /api/inbox/refresh` (`requestRefresh`, 202) — ReloadButton.
  - `POST /api/inbox/extend {before_internal_date}` (`postInboxExtend`, 202) — useInbox auto-extend.
  - `GET /api/tasks?kind=bucket`, `PATCH /api/tasks/{id}`, `DELETE /api/tasks/{id}` (204) — useBuckets' `refresh`/`rename`/`softDelete`, one shared instance owned by `InboxProvider`. The `/api/buckets` shim is unreachable from the current client — it exists only for stale pre-deploy tabs and is deleted in Phase 5 (**Phase 4**: bucket creation moved from the deleted `POST /api/buckets` endpoint to `POST /api/tasks` with `kind="bucket"`, called by `NewTaskWizard` instead of through `useBuckets`. The bucket-draft-preview routes, `POST/GET /api/buckets/draft/preview*`, are deleted along with `NewBucketModal` — superseded by the task-engine goal→draft routes directly below).
  - **New**, Phase 2B — the full task-engine surface (all via the `lib/api.ts` task fetchers, see its Files-table row): `POST /api/tasks/draft {goal}` (202→`{draft_id}`) + `GET /api/tasks/draft/{draft_id}` (pending/ready/gone poll) — `NewTaskWizard` (**Phase 4**: both kinds — bucket creation's goal→draft flow reuses this same pair, `kind` isn't a param on the draft endpoints themselves). `GET /api/tasks` (**Phase 4**: optional `?kind=bucket`, the task-backed replacement for the deleted `GET /api/buckets` list — used by `useBuckets.refresh`, not just `TasksProvider`'s tracker-only default call), `GET /api/tasks/{id}`, `POST /api/tasks` (201, full `TaskDetail`; **Phase 4**: body gains `kind?: 'tracker'|'bucket'`, called by `NewTaskWizard` for BOTH kinds now), `PATCH /api/tasks/{id}`, `DELETE /api/tasks/{id}` (204) — `TasksProvider`, and (**Phase 4**) `useBuckets.rename`/`softDelete`. `GET /api/tasks/{id}/board`, `GET /api/tasks/{id}/events?status&entity_id&page&limit` (default `limit=50` — see Gotchas), `GET /api/tasks/{id}/threads` — `TaskDetail`'s three independent refetches. `POST /api/tasks/{id}/threads {thread_id, add_example}` (201), `DELETE /api/tasks/{id}/threads/{thread_id}?add_example=` (204) — `TaskDetail`'s attach/detach handlers, `add_example` sourced from `ThreadsPanel`'s per-row checkbox. `POST .../events/{id}/approve\|reject\|revert` (200) — `ReviewFeed`'s actions via `TaskDetail`. `POST .../entities/{id}/state {field,value}`, `POST .../entities/{id}/merge {into_entity_id}` — `EntityDrawer`'s inline edit/merge via `TaskDetail`; both surface a 422 `detail` string inline via `throwWithDetail`.
  - **New**, Phase 3 — `GET /api/reviews?limit=` (`getReviews`) and `GET /api/activity?limit=` (`getActivity`) — `ReviewTray`/`ActivityTicker` respectively, each the sole consumer of its route; both are plain synchronous fetches (server clamps `limit`, never 422s) with no SSE payload of their own — the two components instead infer "something may have changed" from `TasksProvider`'s already-live `tasks` list (`summary.pending_reviews`/`version` sums) and refetch directly, rather than the server pushing a new SSE event for these aggregate views.
- **SSE — lib/sse.ts** `EventSource('/api/sse')`. Data events consumed: `threads_updated{thread_ids}` (useInboxSse→useInbox.applyThreadUpdates), `extend_complete{thread_ids,more}` (useInbox). **Phase 4**: `bucket_draft_preview{draft_id,positives,near_misses}` (formerly consumed by `NewBucketModal`) is deleted along with that component — bucket-kind drafts now arrive as `task_draft_ready`, same as tracker-kind. Synthetic conn events `_open`/`_error` broadcast to all handlers. Server `: keepalive` frames (5s) are non-JSON and ignored by `onmessage` try/catch. `_open`/`_error` are also consumed by `AppShell`'s own `subscribeSse` handler purely to drive the auth-recheck error-streak counter (see gotchas) — it never touches inbox state. **New**, Phase 2B — three task events, all consumed by `TasksProvider`'s one subscription (plus `NewTaskWizard`'s own ad hoc subscription for `task_draft_ready` only, **Phase 4**: for both its tracker and bucket modes): `task_draft_ready{draft_id}`, `task_updated{task_id,version,pending_count}`, `task_backfill_progress{task_id,scanned,matched,done}`.
- **Client-side state**: pagination cursor `page` + derived `pageCount` (PAGE_SIZE=50); `more` (history exhaustion); `extendInFlight`; refs `lastInternalDate` (LWW gate per id), `lastExtendAtLength` (no-progress guard), `extendWatchdog` (90s timer) — all owned by the one `InboxProvider`-instantiated `useInbox`, not `InboxPage`; shared `useInboxSearch` `query`/`results`/`error` state + `searchSeq` ref (monotonic token, guards stale async responses), used by `HudPage`, `InboxPage`, and (**new**, Phase 2B) `ThreadsPanel`'s search-to-attach box; `HudPage`'s own `status` state + 30s `refreshStatus` poll and cosmetic 5s ago-ticker `setInterval` (`forceTick`). **Phase 4**: `NewBucketModal`'s `appliedRef` and `InboxPage`'s reclassify `setTimeout`s (60s+150s) are both deleted — `NewTaskWizard`'s own `appliedRef`/`navigatedRef`/`unmountedRef` (below) now cover bucket-mode idempotency too, and bucket backfill's deterministic publish needs no watchdog. **New**, Phase 2B: `TasksProvider`'s `tasks`/`details`/`backfill` state + `byIdRef` (stale-closure guard for its SSE handler); `TaskDetail`'s `entities`/`events`/`threads`/`selectedEntityId` local state; `NewTaskWizard`'s wizard `step`/form fields/`examples` + `appliedRef`/`navigatedRef`/`unmountedRef` (**Phase 4**: shared across both `kind` modes); `ReviewFeed`'s `busy: Set<eventId>` + per-id 10s timeout refs (`busyTimersRef`); `EntityDrawer`'s `error`/`mergeTarget`, reset per `entity.id`. **New**, Phase 3: `ReviewTray`'s own `items: FeedItem[]` + independent `busy`/`busyTimersRef` (same shape as `ReviewFeed`'s, not shared) + `mountedRef` (skip-initial-mount guard for its `totalPending`-keyed refetch effect); `ActivityTicker`'s own `items: FeedItem[]` + `mountedRef` (same guard, keyed on both `totalVersion` and `totalPending`) — neither component's state lives in `TasksProvider`, both are self-contained.
- **fetch vs SSE**: snapshot/hydrate/mutations are fetch; new/changed threads, extend completion, and task draft/update/backfill-progress (**Phase 4**: including bucket-kind creation, which now shares the tracker flow's SSE/poll dance rather than the deleted bucket-draft-preview one) are pushed over SSE, with HTTP polling fallback for the task draft flow.

## Data flows / cross-subsystem touchpoints
- `Browser ──[GET /auth/me, Cookie:session]──> API` — auth bootstrap.
- `Browser ──[GET /api/inbox?limit=200]──> API ──[JSON InboxPage]──> Browser` — snapshot/resync top-N view.
- `Browser ──[GET /api/sse, text/event-stream]──> API` — long-lived; frames `threads_updated`/`extend_complete` + `: keepalive`/5s (**Phase 4**: `bucket_draft_preview` is deleted from this list — see below).
- `Browser ──[POST /api/threads/batch {thread_ids:[…]}]──> API ──[JSON {threads}]──> Browser` — SSE replay/hydrate path (1 round trip for ≤200 ids).
- `Browser ──[POST /api/inbox/refresh]──> API` (202) — kicks a worker Gmail poll; results return later as SSE `threads_updated`. ReloadButton also resyncs locally.
- `Browser ──[POST /api/inbox/extend {before_internal_date}]──> API` (202) — worker extends Gmail history; completion arrives as SSE `extend_complete`.
- **Phase 4**: the two bullets this doc used to carry here — bucket draft-preview (`POST/GET /api/buckets/draft/preview*`) and bucket creation via `POST /api/buckets ──> reclassify_user_inbox` — are both gone. Bucket creation now flows entirely through the task-engine bullets below (`POST /api/tasks/draft`, `POST /api/tasks` with `kind="bucket"`), and its completion (`threads_updated` from `backfill_task`'s bucket branch) is deterministic, so `InboxPage`'s old 60s+150s `resync()` watchdogs are deleted along with it — see INBOX_SYNC_INDEX for the server-side detail.
- `Browser ──[GET /api/search?q=]──> API ──[JSON InboxPage]──> Browser` — shared `useInboxSearch()` debounced (300ms) box (`HudPage` + `InboxPage`); plain synchronous request/response, no SSE involved; `searchSeq` discards a response if a newer query has since superseded it; results replace the mounted page's `<InboxList>` source until the query is cleared.
- `Browser ──[GET /api/sync/status]──> API ──[JSON {last_synced_at, has_cursor}]──> Browser` — `HudPage`'s own `refreshStatus()` fetches this (mount + 30s poll) to drive the freshness strip; plain synchronous request/response; independent of the shared inbox store.
- **Intra-tab (§2.11)**: `InboxProvider`'s single `useInboxSse`, `TasksProvider`'s single subscription (**new**, Phase 2B), `NewTaskWizard` (**new**, task-draft-only — **Phase 4**: the fourth former subscriber, `NewBucketModal`, is deleted), and `AppShell`'s error-streak counter each subscribe the shared `lib/sse.ts` `EventSource` via the `_handlers` Set — one socket fans out to the few React subscribers that remain. The inbox side and the task side are each one shared subscription for the whole authed session; `AppShell`'s is a third, independent handler that only counts `_open`/`_error` and never touches inbox or task data.
- **SSE→state mutation**: `threads_updated`→`getThreadsBatch(ids)`→LWW-gate→merge `displayLayer` + re-sort `idLayer` desc by `internal_date`. `extend_complete`→`setMore`+clear watchdog+apply ids. **New**, Phase 2B — `task_draft_ready`→(`NewTaskWizard`'s own handler, **Phase 4**: for either `kind`) `getTaskDraft(draft_id)`→`applyDraft` (seeds name/description/schema/probes/examples, flips wizard step to `review`); `task_updated`→(`TasksProvider`) version-gap-OR-pending-count-gap check→conditional `loadDetail(task_id)`; `task_backfill_progress`→(`TasksProvider`) updates `backfill[task_id]`, plus `loadDetail` on `done`.
- `Browser ──[POST /api/tasks/draft]──> API ──[{draft_id}]──> Browser` (**new**, Phase 2B; **Phase 4**: also the bucket-creation goal→draft entrypoint) — `NewTaskWizard`; result via SSE `task_draft_ready` (payload is bare `draft_id` — the wizard still has to `GET /api/tasks/draft/{draft_id}` to fetch it) OR polled fallback (starts at 5s, "let SSE win" rationale).
- `Browser ──[POST /api/tasks]──> API` (**new**, Phase 2B) — `NewTaskWizard.submit()`; 201s the full `TaskDetail`; server-side enqueues `backfill_task`, progress arrives as repeated SSE `task_backfill_progress` (consumed by both `TasksProvider`, for the HUD card, and `NewTaskWizard`'s own `progress` watch, for the `creating`→navigate transition) then one terminal `task_updated`.
- `Browser ──[GET /api/tasks/{id}, /board, /events, /threads]──> API ──[JSON]──> Browser` (**new**, Phase 2B) — `TaskDetail`'s four independent GETs (one via `TasksProvider`'s cache, three owned locally), replayed together whenever `TasksProvider`'s SSE-driven refetch actually changes `detail.version`/`detail.summary.pending_reviews`.
- `Browser ──[POST/DELETE .../threads]──> API` (**new**, Phase 2B) — `ThreadsPanel`'s attach/detach (via `TaskDetail`); `add_example` flows through from the row checkbox; completion triggers a local refetch (`refetchThreads`+`refetchEvents` or `refetchAll`), not just a wait for `task_updated` (which may not even bump `version` for these — see Gotchas).
- `Browser ──[POST .../events/{id}/approve\|reject\|revert]──> API` (**new**, Phase 2B) — `ReviewFeed` (via `TaskDetail`); local refetch (`refetchEvents`+`refetchBoard`) drives the UI, not the SSE round trip.
- `Browser ──[POST .../entities/{id}/state, .../merge]──> API` (**new**, Phase 2B) — `EntityDrawer` (via `TaskDetail`); a 422 rejects the awaited promise, rendered inline by the drawer; success triggers a local refetch the same way.
- `Browser ──[GET /api/reviews?limit=]──> API ──[JSON {reviews: FeedItem[]}]──> Browser` (**new**, Phase 3) — `ReviewTray`'s own fetch, unwrapped by `getReviews`; refetched on mount and whenever `useTasksStore()`'s summed `pending_reviews` across all tasks changes.
- `Browser ──[GET /api/activity?limit=]──> API ──[JSON {activity: FeedItem[]}]──> Browser` (**new**, Phase 3) — `ActivityTicker`'s own fetch, unwrapped by `getActivity`; refetched on mount and whenever `useTasksStore()`'s summed task `version` OR summed `pending_reviews` changes (two independent signals — see Gotchas).

## Decision points & gotchas
- **Pagination + auto-extend** (useInbox): `useEffect` fires `requestExtend` when `page >= pageCount-1`, not while a filter is active (filter shrinks pageCount artificially), not when `more===false` or `extendInFlight`. `lastExtendAtLength` ref blocks re-firing at the same `idLayer.length` so a 0-result extend can't tight-loop; re-arms when new ids grow the length. Extend cursor = smallest `recent_message.internal_date` across `idLayer`.
- **Extend watchdog**: `setTimeout(90_000)`; if `extend_complete` never arrives, force-reset `extendInFlight` + clear no-progress guard (no auto-retry — surfaces server bugs). Cleared on event arrival or POST failure.
- **LWW gate** (`applyThreadUpdates`): accept incoming thread only if `recent_message.internal_date >= lastInternalDate[id]` (default 0). `fetchAndReplace` **resets** (not merges) `lastInternalDate` so a thread aged out of the latest-200 window can't keep a stale gate value.
- **Snapshot/replay ordering** (useInboxSse): on `_open`, buffer `threads_updated` until `snapshot()` resolves, then drain in order — prevents an early SSE event being overwritten by a later snapshot. `cancelled` flag guards against re-`_open` racing a prior lifecycle. `_error` clears `ready` so subsequent events buffer again.
- **resync vs snapshot**: `snapshot` toggles `loading` (flashes "loading…"); `resync` (ReloadButton, watchdogs, page-clamp recovery) does NOT — keeps the list on screen. `resync` collapses `idLayer` to latest 200, so a page-clamp `useEffect` snaps `page` back to `pageCount` to avoid a blank extended-history page.
- **Optimistic vs server-confirmed**: nothing optimistic — `useBuckets` mutations refetch (`GET /api/buckets`) before UI updates; inbox rows always come from server via fetch/batch.
- **Draft preview dual-resolution — now goal→draft, and shared by both wizard kinds** (**rewritten**, Phase 4 — supersedes the old "NewBucketModal" entry): SSE fast path + 5s poll fallback (poll starts at 5s to let SSE win the scoring window), the same shape `NewBucketModal`'s bucket-draft-preview flow used before it was deleted — `NewTaskWizard`'s `appliedRef` per `draft_id` makes apply idempotent regardless of arrival order; `gone` (404, redis TTL-expired) surfaces inline via `draftError` + a `back` button rather than stranding the user. This flow is now the ONLY goal→draft dance in the client — bucket-kind creation reuses it verbatim (see that file's row) rather than having its own separate preview mechanism.
- **One shared `useBuckets`/`useInbox` instance for the whole authed session, not per route**: `InboxProvider` (mounted once by `AppShell`) is the only place `useBuckets`/`useInbox`/`useInboxSse` are called; `HudPage` and `InboxPage` both consume the same instances via `useInboxStore()`. Renaming/deleting a bucket on `/inbox` live-updates `HudPage`'s bucket cards immediately (no "refresh only on next mount"), and navigating between `/` and `/inbox` no longer remounts-and-refetches the inbox snapshot — `inbox.loading` only flashes true on the first-ever mount per session. Within `InboxPage`'s subtree, `ViewBucketsModal` still takes callbacks from the store's `buckets` object rather than calling `useBuckets()` itself — a second instance anywhere would refresh nobody's rendered list, leaving toolbar/filter stale. **Phase 4**: bucket *creation* no longer goes through this store at all — `NewTaskWizard kind="bucket"` calls `useTasksStore().createTask` directly and its own `onCreated` callback (not a `buckets`-object prop) is what triggers `buckets.refresh()` afterward.
- **StaticFiles / dev-proxy split**: prod — Vite builds into `server/app/static/` (`VITE_OUT_DIR` override), served by FastAPI `StaticFiles` + SPA catch-all (same origin, no proxy). Dev — Vite dev server `:5173` proxies `/auth` and `/api` to `http://localhost:8000` (`vite.config.ts`); `same-origin`/`withCredentials` cookies work because the proxy keeps one origin.
- **401 handling**: `getJSON`/`getThreadsBatch` throw `{kind:'unauthorized'}`; `useAuth.refresh` maps any error to `anon`. Other hooks (`useInbox`, `useBuckets`) do not specially route 401 — they log/swallow.
- **Archived-thread eviction, not a flag** (useInbox, **new**): `applyThreadUpdates` splits LWW-accepted threads into archived vs live; archived ids are deleted outright from `displayLayer`/`idLayer`/`lastInternalDate` — there's no rendered "archived" state, the row just disappears on the next SSE-driven batch fetch. This mirrors the server's reconciling archive/un-archive (see INBOX_SYNC_INDEX) but the client only ever sees the one-way "thread left the inbox" transition; an un-archive re-adds the thread as an ordinary live merge (it's just `is_archived:false` on the next accepted update).
- **Search race guard, not cancellation** (`useInboxSearch`, shared by `HudPage`/`InboxPage`): the 300ms debounce timer is cleared per-keystroke, but an in-flight `searchInbox` fetch is never aborted — `searchSeq` just discards its result if a newer query (or a clear) has since superseded it (`seq !== searchSeq.current`). Clearing the box bumps `searchSeq` too, so a slow stale response can't repopulate results after the user backs out.
- **Search bypasses `useInbox` entirely (both routes)**: `results` is a flat one-shot array from `GET /api/search`, not merged into `idLayer`/`displayLayer`. While search is active, the shared `useInbox` instance's pagination/auto-extend/bucket filter keep running in the background but are invisible until the query is cleared and `<InboxList>` switches back to `inbox.pageThreads` (`InboxPage`) or the store-derived `recent`/bucket-count views (`HudPage`). **Updated**: `HudPage` now does have a live `useInbox` instance (the shared one, via `useInboxStore()`) but still reads it only for `idLayer`/`displayLayer`, never `page`/pagination — its search simply swaps `<InboxList>`'s source the same way it always did.
- **The SSE-pinning gotcha is gone structurally, not by convention** (**rewritten** — supersedes the old "AppShell pins the SSE singleton open" entry): `lib/sse.ts`'s `EventSource` is reference-counted by handler count — `_es` closes when the last `subscribeSse` handler unsubscribes and reopens fresh on the next subscribe. Previously, `AppShell` had to hold a dedicated no-op handler because the two routes' own subscriptions (`HudPage`'s direct `subscribeSse` call, `InboxPage`'s `useInboxSse`) each mounted/unmounted with their route, so a cross-route navigation could transiently drop the handler count to zero between the outgoing route's cleanup and the incoming route's mount — closing and reopening the connection (plus an `active_users` deregister/re-register blip) on every navigation. Now `InboxProvider`'s single `useInboxSse` subscription lives at the `AppShell` layer for the entire authed session — it mounts once and only unmounts on sign-out (`AppShell` unmount → `InboxProvider` unmount → last unsub → close). Neither route mounts or unmounts its own SSE handler anymore, so the subscriber count never transiently hits zero during `/`↔`/inbox` navigation: the close/reopen churn risk is eliminated by construction, not papered over by a pin.
- **SSE error-streak → 401-only session recheck** (**new**, `AppShell` + `useAuth.recheckSession`): an expired session cookie makes every SSE reconnect fail with a fresh 401 forever — nothing about the stream alone distinguishes "the connection is down" from "the session is dead." `AppShell` counts consecutive `_error` events (reset by any `_open`); at 3 or more in a row, throttled to once per 30s, it calls `recheckSession()` — a `useAuth` method distinct from mount-time `refresh()` but sharing its `fetchMe()` helper. `recheckSession` flips auth state to `'anon'` **only** when the failure is a definitive 401 (`{kind:'unauthorized'}`, `lib/api.ts`'s shape for a real 401); any other failure (network outage, 5xx, timeout) leaves state untouched and the SSE backoff keeps retrying — a backend outage must not force-logout a session that's still valid. This deliberately differs from `refresh()`'s mount-time catch-all (any failure → `anon`), which is correct there because a failed initial load has no session to preserve. A real 401 flips `Gate` to `anon` → `<Login>` replaces `<AppShell>`/`<InboxProvider>` in the same commit, unsubscribing the last SSE handler and closing the stream.
- **HudPage refresh cadence — status-only now** (**updated**): `HudPage` no longer fetches its own inbox snapshot or listens for `threads_updated`/`_open` — the shared store (`InboxProvider`'s `useInboxSse`→`useInbox`) keeps `idLayer`/`displayLayer` current independently of which route is mounted, so `HudPage`'s "recently processed" list and bucket counts just re-render off the store's state (no debounce timer needed). `HudPage`'s own effect only drives sync-status cosmetics: an initial `getSyncStatus()` fetch, a 30s `refreshStatus` poll, and a 5s `setInterval` (`forceTick`) that re-renders `agoLabel` between polls — two intervals, no unsub, no debounce timer.
- **A task `version` gap alone is NOT a sufficient refetch trigger — `pending_count` is an equally load-bearing second signal** (**new**, Phase 2B; confirmed against server behavior in TASKS_INDEX): `TasksProvider`'s `task_updated` handler only calls `loadDetail(task_id)` when `e.version > known.version OR e.pending_count !== known.summary.pending_reviews`. This OR is required, not defensive boilerplate — per TASKS_INDEX's gotchas, several real mutations publish `task_updated` **without** bumping `version` at all: `reject_event`, `attach_thread`/`detach_thread`'s criteria-only `append_example` write (Phase 2B Task 2), and detach's new auto-reject-pending-events step (Phase 2B Task 1) all move `pending_count` (reject/auto-reject) or mutate the task in a way invisible to `version` (criteria) while leaving `version` untouched. A client that checked `version` alone would silently miss all of these — the review-queue badge (`task.summary.pending_reviews`, rendered on both the HUD's task cards and implicitly via `TaskDetail`'s refetch-cascade effect) would go stale exactly on a reject.
- **The learning loop is entirely server-side and invisible to the client** (**new**, Phase 2B Task 2): `Task`/`TaskDetail`'s TypeScript shape (`lib/api.ts`) has no `criteria` field at all — `append_example`'s tagged `<positive>`/`<nearmiss>` blocks (and the FIFO cap at `EXAMPLE_CAP=30`) live purely in a Postgres column the client never fetches. The "teach the task" checkbox (`ThreadsPanel`'s `AttachedRow`/`SearchResultRow`, default checked, wired to `attachThread`/`detachThread`'s `addExample` param) is the only client-visible trace of this feature — there is no UI anywhere that shows the accumulated example text, its current count, or when the FIFO cap starts evicting old corrections.
- **`EntityDrawer`'s per-entity history is bounded by `TaskDetail`'s events fetch, not by entity** (**new**, Phase 2B — verified gotcha): `TaskDetail.refetchEvents` calls `getTaskEvents(taskId)` with no `limit`/`page` args, so the server default (`GET /api/tasks/{id}/events` → `limit=50`, page 1, newest-first across the **whole task**) is what both `ReviewFeed` and `EntityDrawer` filter client-side. `EntityDrawer`'s `entityEvents` (`events.filter(e => e.entity_id === entity.id)`) can therefore under-represent a busy entity's true history on a task with many entities/events: once the task has accumulated more than 50 events total, an entity whose own events have aged out of that newest-50 window shows an incomplete (or empty) "history" section with no indication anything was truncated — there's no "load more"/pagination affordance in the drawer, even though `getTaskEvents` itself supports `page`/`limit`. `ReviewFeed`'s "Recent activity" list further slices this same already-capped 50 down to 30 non-pending entries.
- **`ReviewTray`/`ActivityTicker` are self-owned-data components, deliberately outside `TasksProvider`'s cache** (**new**, Phase 3): both fetch `getReviews()`/`getActivity()` directly on mount rather than being fed by `useTasksStore()` — the cross-task aggregate feed has exactly one consumer each, so there's no cache to share. They still READ `useTasksStore()`'s `tasks` list, but only to derive a summed scalar (`totalPending` for `ReviewTray`; `totalVersion` AND `totalPending` for `ActivityTicker`) that tells them "something changed somewhere, go refetch" — they never read `tasks[].summary`/`.version` for display, and approving/rejecting from inside either component never touches `TasksProvider`'s state directly (that store still only updates via its own SSE `task_updated` handler). A change made inside a task's own `ReviewFeed`/`EntityDrawer` is what surfaces here indirectly, through the same SSE path.
- **Why `ActivityTicker` sums TWO signals (`totalVersion` and `totalPending`) but `ReviewTray` sums only one (`totalPending`)** (**new**, Phase 3): `ReviewTray`'s feed is exactly the pending-review set, and the one thing that ever moves an event into or out of it (a new LLM proposal, or an approve/reject resolving one) always moves `pending_reviews` — one signal suffices. `ActivityTicker`'s feed is the OTHER set (applied/rejected/reverted) — a `reject` produces a brand-new item in THIS feed (`status="rejected"`), but per TASKS_INDEX's `bump_version` contract, `reject_event` is the one mutation that does **not** bump `task.version` ("server publishes the pending_count change only by design," per the component's own comment) — so `totalVersion` alone would never notice a reject happened and the ticker would silently miss showing it. `totalPending` (the same signal `ReviewTray` already uses) catches rejects; `totalVersion` catches everything else (applies/edits/approves). Both are kept as separate `useEffect` dependencies rather than combined into one arithmetic sum, because an approve's `version +1` and `pending_reviews -1` would otherwise net to zero and silently skip a needed refetch.
