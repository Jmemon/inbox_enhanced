

## inbox sync
number pages based on how many threads per page and how many threads are in the inbox (like the gmail webapp does).

for any active users maintain a websocket connection  between them and the backend to request next page of messages, maybe to maintain some notion of user state to load htings faster. 

So i'll need to be downloading email threads (with attachments) to the backedn

### notes
for hud purposes its more critical to keep gmail and our backend in sync.so we can display task statuses and threads associated with tasks with email snippets to help display task updates. 
and also neeod the inbox to correct any mistakes in the pipelines (ie missed that should be counted)
I think the hud view will allow us to scroll through the inbox, but that won't be default. this does mean we need to ensure the inbox state is kept in sync between gmail<>backend<>frontend.
gmail<>backend and backend<>frontend are different though.

When user is active can rely on the existing enqueue_poll beat job and the processing workers.
But in general when user is inactive I think there should be a less frequent (every hour) poll across all users in database.



## Inference Client
switch llm client to litellm so we can use cheaper inference providers or one we host

